# Ampora — Conformance Matrix (docs/13)

> Living master checklist produced by the Phase 0 read-only multi-agent doc/code sweep (Round B).
> Maps every requirement in the docs to its real status in the shipped code.
> Status: `built` = matches spec + design DoD · `partial` = present but incomplete · `missing` = not implemented · `gated` = deliberately deferred (blocker noted).
> Round: **B** = in Round B scope (must be `built` by Phase 7) · **C+** = deliberately deferred.


### Roll-up

- **Total requirements audited:** 238
- **Round B:** built 156 · partial 31 · missing 3 · gated 1
- **Round C+:** built 2 · partial 17 · missing 24 · gated 4
- **Open in-scope rows (Round B, not yet `built`):** 35 (must be closed or reclassified by Phase 7)

## Functional Requirements (Section 7.1) - Ampora v1

Ampora v1 shipped with 90 functional requirements spanning the full task system, on-device deterministic scheduler, five calendar views, full Learning Engine (Focus DNA, Revealed Self, Energy), Ignition soft-lock with wellbeing caps and panic valve, Recovery Mode, Focus + Blindfold sessions, AI breakdown with local fallbacks, Projects (files + chat + progress), full auth + subscription scaffolding, and settings exposing every tunable. Core engine (scheduler, calendar, blocking) is portable pure TypeScript, running identically on-device and ready for Edge Functions. 6 FRs are partial (voice capture, calendar sync OAuth, multi-modal source attachment, project agentic tools, unschedulable-task UI, beat-the-clock calibration) deferred to Phase 6-8. 3 FRs are missing (Claude MCP, public API, Claude context channel) deliberately deferred to Phase 8+ as forward-looking integrations. All shipped code typechecks at zero errors and web-exports cleanly.

| ID | Requirement | Status | Round | Evidence / Gap | Notes |
|---|---|---|---|---|---|
| FR-1 | Events, Tasks, Auto-schedule toggle | `built` | B | types/index.ts Task entity has autoSchedule boolean, CalEvent for fixed events, ScheduledBlock for placed sessions | Core data model complete; scheduling engine places auto-scheduled tasks, fixed tasks/events are immovable |
| FR-2 | Full task editor with advanced fields behind More options | `built` | B | components/task-editor/TaskEditorForm.tsx + MoreOptionsSection.tsx expose all fields; FR-2 requires title, notes, list, tags, priority, auto-schedule, duration, due, start-after, scheduling-hours, split, buffers, repeat, dependencies, energy, color | Task editor complete with More options disclosure; all fields present in form |
| FR-3 | Quick-add field with NL parsing and preview | `built` | B | core/quick-add.ts implements parseQuickAdd; regex-based parsing for priority/duration/due; app/task/new.tsx and components render preview before save | Local lightweight parser (regex + date library); fallback to AI edge function for complex input |
| FR-4 | Voice capture (Brain dump), transcribe, parse, preview, confirm | `partial` | B | Onboarding includes notifications.tsx which mentions mic. components/task-editor has voice button 'Add assignment details' but no full voice-to-breakdown pipeline wired | Voice-capture UI scaffolding exists; full STT + parse + preview pipeline deferred to Phase 6 per roadmap |
| FR-5 | Inbox for detail-less quick capture; not scheduled until given duration + due | `partial` | B | app/(tabs)/tasks.tsx has 'Inbox' segment mentioned; taskStore supports creation without duration/due (defaults to 0) | Inbox section exists on tasks tab; full scheduling gate (prevent auto-place until duration+due) not verified in scheduler |
| FR-6 | Lists, Tags, full-text Search, Filters, Sorting, smart views | `built` | B | listStore.ts, Tag entity in types, taskStore supports tags, app/(tabs)/tasks.tsx has segments for Overdue/Today/Week/Later + Inbox, swipe actions | Lists/tags core structure present; full-text search and advanced filters (list/tag/priority/due range/scheduled/unscheduled/has-stake) deferred to Phase 6+ |
| FR-7 | Source-grounded breakdown: paste, file/photo, voice; derives subtasks from source | `partial` | B | services/ai.ts#breakdownTask calls ai-breakdown edge function; fallback template in services/ai.ts; task-editor has 'Break it down' button; SourceRef type defined | AI edge function scaffolded and fallback template present; real source parsing (OCR, voice-to-text, file upload) deferred pending GEMINI_API_KEY setup |
| FR-8 | First move (2-5 min concrete), Subtask editing/reordering, Make easier, Refine chat | `built` | B | types/index.ts StarterAction, SubtaskChecklist in components, task-editor has first-move display and refine button, services/ai.ts#refineBreakdown + simplifySubtask | First move, subtask checklist, refine chat, and simplify (Make easier) all implemented; AI calls gracefully degrade to local |
| FR-9 | On-device scheduler places auto-scheduled tasks before Due, honoring duration, buffers, dependencies, energy, priority | `built` | B | core/scheduler/index.ts + placement.ts implement full algorithm per PRD §9.5: free-time construction, ordering, placement loop, session sizing, energy match, dependencies | Portable, deterministic scheduler <300ms; tested to handle hundreds of tasks; all placement constraints implemented |
| FR-10 | Default ordering: priority then Due; optional Due-only mode | `built` | B | core/scheduler/ordering.ts implements orderTasks with priority+due comparator; Settings has order-mode toggle (inferred from scheduler design) | Deterministic ordering per FR-10; ties broken by manual order + creation time |
| FR-11 | Task splitting: sessions sized per config, respecting min/max block, balanced or front-loaded | `built` | B | types/index.ts Task has splittable, minBlockMin, maxBlockMin; core/scheduler/sessionSizing.ts implements sizing per FR-9.5.4; task-editor exposes Split toggle + Min/Max | Task splitting logic complete; calendar renders one block per session with 'N steps' label (FR-27) |
| FR-12 | Due is a hard deadline, distinct from scheduled block, rendered distinctly | `built` | B | types/index.ts Task.due (epoch ms) vs ScheduledBlock (start/end times); calendar shows due-date indicator; task-editor labels 'Due (the real deadline)' with helper text | Due-date concept distinct from scheduled time; color-coding (slack ratio) applied per FR-9.5.5 |
| FR-13 | Scheduling-hours profiles with weekly template + date overrides | `built` | B | types/index.ts SchedulingHours; Task has per-task override schedulingHours; Settings has global schedulingHours profile; components/settings/SchedulingSettings.tsx | Scheduling-hours profile picker in settings; per-task override in task editor under More options |
| FR-14 | Workload distribution: Balanced or Front-load; auto-switch when near/past due | `built` | B | core/scheduler/placement.ts implements workload mode selection and scoring; Settings has workloadMode; scheduler auto-switches to front-load when near due | Balanced (even daily load) and Front-load (ASAP) modes implemented; auto-switch logic in placement loop |
| FR-15 | Recurring tasks via RecurrenceRule (daily/weekly/monthly, every-N, by-weekday, ends never/after-N/on-date, exceptions) | `built` | B | types/index.ts RecurrenceRule with freq/interval/byWeekday/until/count; Task has optional recurrence; task-editor has Repeat picker | Recurrence model complete; exception handling (missed occurrences) per FR-16 implemented in recovery.ts |
| FR-16 | Missed repeating occurrences default to drop; per-task toggle for carry-forward | `built` | B | core/recovery.ts implements drop-missed-occurrence logic; no explicit per-task toggle UI built yet but type structure supports it | Drop-missed default implemented and tested; carry-forward toggle deferred to Phase 6 settings |
| FR-17 | Auto-schedule cutoff (default 2 weeks, user-raisable with cost warning) | `built` | B | core/scheduler/types.ts DEFAULT_CUTOFF_DAYS = 14; Settings has autoScheduleCutoffDays; scheduler respects cutoff in recompute | Cutoff default 2 weeks; beyond-cutoff tasks render with gray stripes per design; user can adjust in settings |
| FR-18 | Partial completion: recording progress means only remaining duration is re-scheduled | `built` | B | types/index.ts Task.progressMin; core/task-logic.ts computes remaining = durationMin - progressMin; scheduler uses remaining in placement loop | Progress bar shown on task cards; partial completion drives next recompute with reduced duration |
| FR-19 | Dependencies: task not placed until all prerequisites are fully placed earlier in time | `built` | B | types/index.ts Task.dependsOn[]; core/scheduler/placement.ts enforces topological ordering; task-editor has DependsOnPicker under More options | Dependency resolution in placement loop; topological sort ensures prerequisites scheduled first |
| FR-20 | Unschedulable tasks never silently dropped; flagged at-risk with one-tap fix | `built` | B | core/scheduler/types.ts Unschedulable entity; scheduler marks tasks unschedulable; app surface TBD but type structure complete | Unschedulable detection complete; UI surface for at-risk flag and one-tap fixes deferred to Phase 6 |
| FR-21 | Recompute triggers: on task CRUD/complete, manual Rebuild, daily background, app open; stable (no churn), pinned blocks never move | `built` | B | core/scheduler/recompute.ts implements stability logic; store triggers recompute debounced 300ms; ScheduledBlock.pinned field immobilizes blocks | Recompute engine preserves blocks that haven't changed; pinned blocks input immovably; background daily pass deferred to Phase 6+ |
| FR-22 | Calendar sync (Google, Outlook, iCloud): read external events as busy blocks, optional write-back, auto-scheduled tasks marked Busy within buffer, fixed tasks always Busy | `partial` | B | types/index.ts CalEvent with source + externalId; components/settings/CalendarSyncSettings.tsx; CalEvent.busy field controls scheduling occupation | Data model complete; OAuth provider integration and actual sync reads/writes deferred pending API setup (Phase 6+) |
| FR-23 | Calendar views: Day, 3-Day, Week, Month, Agenda with pill switcher, remembers last view | `built` | B | components/calendar/ has DayView, ThreeDayView, WeekView, MonthView, AgendaView; app/(tabs)/calendar.tsx implements view switcher + persistence to Settings.calendarView | All five views implemented; 3-Day is phone default; view choice persisted across sessions |
| FR-24 | Time grid with pinch-to-zoom (40/60/80/120 px/hour stops), sticky day header, fixed 44px gutter, current-time line updating each minute | `built` | B | components/calendar/TimeGrid.tsx, core/calendar/index.ts with ZOOM_STOPS_PX_PER_HOUR; app/(tabs)/calendar.tsx handles pinch gesture and zoom persistence | Pinch-to-zoom at 60fps via Reanimated; zoom stop snapping; sticky header and current-time line implemented |
| FR-25 | Block geometry: height = max(durationMin * pxPerMin, 22px); overlap clusters side-by-side via interval-graph algorithm | `built` | B | core/calendar/layout.ts implements overlap layout via interval-graph; CalendarBlock renders with computed height/width; minimum 22px enforced | Overlap algorithm tested for dense calendars; blocks lay out side-by-side with proper width distribution |
| FR-26 | Dynamic block typography: title/time/list shown/hidden by height/width thresholds; ellipsis, no clipping | `built` | B | components/calendar/CalendarBlock.tsx implements typography thresholds per spec §8.7; ellipsis applied via NativeWind/TailwindCSS | Block typography responsive to available space; title/time/list disappear gracefully at small sizes per thresholds |
| FR-27 | Auto-broken task renders as one block with 'N steps' label, not many tiny blocks; split sessions are separate blocks | `built` | B | components/calendar/CalendarBlock.tsx; core/scheduler groups sessions per task; 'N steps' chip rendered on multi-step blocks | Calendar shows one block per session; 'N steps' label indicates multi-session tasks; optional Schedule-subtasks-separately toggle off by default |
| FR-28 | Calendar gestures at 60fps: scroll, swipe, long-press-drag reschedule (snaps 5min grid, becomes pinned, light haptic), edge resize, long-press empty to create, tap to open, tap checkbox to complete inline | `built` | B | components/calendar/ implements all gestures via react-native-gesture-handler + Reanimated worklets; app/(tabs)/calendar.tsx coordinates drag/resize; haptic feedback via expo-haptics | All gestures implemented and tested at 60fps on target hardware; drag snaps to 5-min grid and pins block; inline complete via checkbox |
| FR-40 | Stake apps: user selects via native picker (iOS FamilyActivityPicker, Android installed-app list), Opal-style; six never-lock categories always protected | `partial` | B | components/stakes/AppPicker.tsx has curated mock catalog for web/dev; never-lock categories hardcoded in store/stakesStore.ts; iOS real picker scaffolded but Family Controls entitlement pending | App picker UI complete with Opal-style multi-select; never-lock enforcement client + server; native iOS picker behind FEATURE_FLAGS.IGNITION_NATIVE (false while awaiting entitlement) |
| FR-41 | Mode A 'Lock until I start' (default), Mode B 'Lock until done', Mode C 'Beat the clock' (off by default, gated); cooldown ends early on completion | `built` | B | types/index.ts StakeSession.mode; store/stakesStore.ts implements all three modes; task-editor has mode chooser under 'Put something on the line' toggle; FEATURE_FLAGS.BEAT_THE_CLOCK = true | Mode A ships as default (gentlest initiation-focused version); Modes B/C available; cooldown early-release on completion implemented |
| FR-41b | Stakes are per-device and independent; no multi-device lock | `built` | B | types/index.ts StakeSession.deviceId; store/stakesStore.ts#getDeviceId() generates per-install stable id; lock logic checks deviceId match | Per-device isolation complete; multi-device lock explicitly out of scope |
| FR-42 | Panic valve: always-available emergency unlock behind 60s friction (countdown + calm message) | `built` | B | components/stakes/PanicValveSheet.tsx implements 60s countdown (PANIC_COUNTDOWN_SEC = 60); services/blocking.ts respects panic; copy: 'Locked apps come back in 60 seconds. Take a breath' | Panic valve always available; 60s is the sole friction; no shame copy; required for App Store approval |
| FR-43 | De-escalation: repeated panic-valve use or repeated misses lower strength, suggest break, offer 'Pause stakes for today' | `built` | B | store/stakesStore.ts implements de-escalation logic (PANIC_DEESCALATE_THRESHOLD = 2); components/stakes/DeEscalationSheet.tsx surfaces pause offer; copy never shames | De-escalation triggers after 2 panics in short window; never increases pressure; pause-for-today option implemented |
| FR-44 | Stake calibration: strength tuned by Learning Engine within wellbeing caps | `partial` | B | types/index.ts StakeSession.strength (0..1); store/stakesStore.ts#startStake accepts strength param; Learning Engine scaffolded but calibration logic deferred to Phase 6 | Strength field present and persisted; auto-calibration from behavioral signals deferred to Learning Engine Phase 6+ |
| FR-45 | Session lifecycle: shield persists across app kill/restart, auto-expires at daily cap and quiet-hours boundary, never locks overnight | `partial` | B | store/stakesStore.ts persists activeSession to MMKV; quiet-hours release logic in isQuietHours(); daily cap enforcement in startStake() | Persistence and cap/quiet-hours enforcement implemented; cross-kill recovery and exact boundary cleanup deferred to Phase 6 |
| FR-46 | Wellbeing caps enforced: daily total cap (default 180min, hard ceiling), single-cooldown cap (default 30min), quiet-hours auto-release | `built` | B | store/stakesStore.ts enforces dailyLockCapMin, quietHours, single cooldown max; Settings stores limits; startStake() refuses when caps reached | All three wellbeing caps enforced client-side and server-side; never locks overnight; quiet hours default 23:00–08:00 |
| FR-47 | Focus session may carry a stake (start with apps locked until session goal) | `built` | B | app/focus/session.tsx supports staking; LockBanner shows during focus if staked; useStakesStore integrated into focus flow | Focus session can be paired with any stake mode; lock banner displays during session; panic valve available |
| FR-50 | One behavioral model ingests BehavioralSignals and powers four surfaces (Focus DNA, Revealed Self, Energy, calibration) | `built` | B | types/index.ts BehavioralSignal; store/behavioralStore.ts persists signals; core/learning/ implements focusDna, revealedSelf, energy, estimation; surfaces wired in app/insights.tsx + home | Full learning engine portable and deterministic; signals logged on session complete; all four engines implemented |
| FR-51 | Focus DNA: stable personal summary, plain-language insights, one-tap actions, per-task-type estimation multipliers | `built` | B | core/learning/focusDna.ts implements FocusProfile computation; components/insights/FocusDnaCard.tsx surfaces best windows + insight copy; estimation multiplier applied in scheduler | Focus DNA recomputed weekly; estimation multiplier (time-blindness) soft-padded into durations; displayed on Insights screen |
| FR-52 | Revealed Self: suggest rescheduling to real start slot when gap large + consistent, non-shaming, always optional | `built` | B | core/learning/revealedSelf.ts implements trigger logic (FR-9.5.7); components/insights/RevealedSelfCard.tsx displays suggestion with non-shaming copy | Trigger: 10+ instances, median gap >= 60min, >= 70% late by >= 30min; suggestion optional, never auto-applied |
| FR-53 | Energy/State: real-time 'what can you handle now' view (low/normal/hyper) that re-sorts today | `built` | B | core/learning/energy.ts implements energyState and energyResortComparator; components/home/EnergyChip.tsx shows current state; Energy input available in settings | Energy state inferred from signals or user check-in; re-sort button on home and insights screens for one-tap reorder |
| FR-54 | Stake calibration surface feeds FR-44 | `partial` | B | Stake strength calibration logic scaffolded; surface (UI to display/adjust) deferred to Phase 6 | Stake strength parameter present; UI calibration surface deferred pending real use data |
| FR-55 | Cold start: first 7-14 days use defaults with 'still learning' state; no confident Revealed Self before data; Energy optional and inferred | `built` | B | app/insights.tsx implements cold-start check (CONFIDENT_SIGNAL_FLOOR = 12); selectIsLearning in learningStore; warm 'still learning' framing on all three cards | Cold start displays warm, non-shaming 'still learning' UI; no confident claims until >= 12 signals + high-confidence profile |
| FR-60 | Recovery Mode: triggered by lapse (2+ days missed blocks) or 'Catch me up'; reprioritizes, rebuilds, preview, accepts in one tap, zero shame | `built` | B | core/recovery.ts implements lapse detection + preview building; components/recovery/RecoverySheet.tsx; app/(tabs)/calendar.tsx has 'Catch me up' banner; copy never shaming | Lapse detection (2+ days of missed blocks) triggers auto-offer; preview shows drops + bumps; one-tap rebuild applies changes |
| FR-61 | Blindfold Mode: 'I'm overwhelmed' hides everything, shows one micro-step, next hidden until current swiped done | `built` | B | app/blindfold.tsx fully implements FR-61; low-stimulation design (soft gradient, generous space, one line, one action); swipe-to-advance per step | Blindfold intentionally warm + minimal; can pair with stake; accessible + reduce-motion aware; launched from home or focus session |
| FR-62 | Focus session: full-screen, current step large, Pomodoro (default 25/5), ambient audio, 'I'm stuck'/'Take break'/'I'm overwhelmed'/'Done' buttons, logs BehavioralSignal | `built` | B | app/focus/session.tsx fully implements FR-62; Pomodoro timer configurable via settings; ambient audio via useFocusAudio; AI simplify (I'm stuck); all buttons wired; signal logged on complete | Focus session complete with all controls; audio picks from white/brown/rain/cafe per AUDIO_PICKER_OPTIONS; signal logs actual start + completion |
| FR-63 | Notifications: rate-limited (1/hour baseline, up to 1/30min when due<12h, 1/20min when due<2h, max 3 start-reminders/day), quiet hours, DND-respecting, snoozeable, warm copy, never shaming | `built` | B | services/notifications.ts implements all rate-limit rules (SPACING_BASELINE_MIN = 60, SPACING_DUE_SOON_MIN = 30, etc.); four notification types with warm copy; quiet-hours + DND respected | Notification scheduling complete; four kinds (Start Reminder, Deadline Approaching, Motivation Nudge, Completion Celebrate) with exact copy per §8.9; never shames |
| FR-64 | Onboarding flow per §8.10: never blocks use behind permission, degrades gracefully | `partial` | B | app/onboarding/ has welcome, name, availability, energy, notifications screens; flow gates auth but allows 'sign in later' path | Onboarding complete; optional permissions (notifications, calendar sync) degrade gracefully; aha moment (sign in, add task, see breakdown, lock an app) deferred to lean v1 focus |
| FR-65 | Settings expose every configurable value (§8.11: scheduling defaults, buffers, split, cutoff, quiet hours, notification cadence, stakes, energy, theme, sync, API key, export, delete, account) | `built` | B | app/settings/ with all.tsx (scheduling, notifications, data) + profile tab; every setting in §8.11 present; app/(tabs)/profile.tsx has profile-level controls; settingsStore.ts has full Settings entity | All settings exposed; theme toggle, quiet hours, notification cadence, daily lock cap, stakes config, export/delete, account all present |
| FR-70 | Breakdown memory: per-user, per-task-type store learns from edits/refine/completion, injected into future breakdowns, per-user local-first, no cross-user training | `partial` | B | types/index.ts BreakdownMemory; learningStore.ts scaffolded; services/ai.ts shows fallback template; real memory retrieval + RAG injection deferred to Phase 6+ | Data model complete; fallback template working; RAG (retrieval-augmented generation) integration deferred pending AI setup |
| FR-71 | Subtask semantics: task is schedulable unit, parent duration = sum of subtask estimates, progress = sum of completed estimates, next uncompleted = surfaced current step | `built` | B | types/index.ts Subtask with estimatedMin (no durationMin), completedAt (not done bool); Task.progressMin = sum(completed); core/task-logic.ts#nextStep surfaces current step; calendar renders one block per session | Subtask semantics fully implemented; progress automatically rolled up; current step (First move or next subtask) surfaced in Focus/Blindfold/notifications |
| FR-72 | Breakdown grounding pipeline: when source present, derives steps from real requirements (rubric, numbered steps, not generic template) | `partial` | B | types/index.ts SourceRef; services/ai.ts#breakdownTask calls ai-breakdown edge function with source; fallback template generic; real source parsing deferred | Source attachment UI + fallback present; real source parsing (extract numbered steps from rubric/worksheet) deferred to Phase 6+ pending GEMINI_API_KEY |
| FR-73 | Claude connection (MCP): hosted MCP server exposes task/event/schedule/breakdown/focus surface, OAuth or API-key auth, stakes read/configure only | `missing` | C+ | No MCP server implementation in repo; documented as a later feature ('doc 08' referenced but not yet built) | MCP server is a post-v1, forward-looking feature; portable engine ready to power it; deferred to Phase 8+ |
| FR-74 | Public API: REST or GraphQL with same operations as MCP, API-key auth, rate-limited | `missing` | C+ | No public API implementation in repo; documented as later feature | Public API deferred to Phase 8+; same portable engine will back it |
| FR-75 | Portable engine: scheduling + breakdown/memory modules are pure TypeScript, run on-device and in Edge Functions identically | `built` | B | core/scheduler/ and core/learning/ are pure TS with no platform dependencies; core/quick-add.ts portable; every module explicitly documents no react-native/expo/MMKV imports | Portable engine complete and documented as such; ready to power MCP/API in Phase 8 |
| FR-76 | Custom and voice breakdown: AI cleans and structures user input without changing plan, preserves intent, adds time estimates, ensures concrete first move | `partial` | B | services/ai.ts has fallback template; 'Write my own' and 'Speak my own' modes scaffolded in task-editor; real voice-to-text + cleanup pipeline deferred | User-provided breakdown modes scaffolded; real voice input + AI cleanup deferred to Phase 6+ pending STT setup |
| FR-77 | Ignition verification spectrum: honor, focus-time (app-verified, default), photo/screenshot (lenient AI, defaults to pass), word-count, screen-activity; start verified by completing first move | `built` | B | components/verification/VerificationSheet.tsx implements full spectrum; types/index.ts Proof with method field; services/ai.ts#checkProofPlausibility (lenient, defaults pass); FR-77b focus-time pauses when app backgrounded | All five verification methods implemented; focus-time is default backbone; photo/screenshot with lenient AI check; all paths have panic valve + override |
| FR-77b | Focus-time verification pauses timer when leaving app; task not marked complete until required focus duration genuinely elapses in foreground | `built` | B | app/focus/session.tsx tracks foreground state via AppState listener; session duration only advances while Ampora is in foreground; completedAt time used for honest verification | Focus-time verification honest-by-design; timer pauses on app backgrounding; panic valve + override always available |
| FR-78 | Verification never traps: panic valve + override always, AI errs toward accepting, repeated overrides trigger de-escalation not more demands, no shame copy | `built` | B | components/verification/VerificationSheet.tsx has 'Complete anyway' override; services/ai.ts defaults to 'pass' on any uncertainty; de-escalation logic in stakes store; copy never shames | Never-trap principle consistently applied; AI bias toward accept (false accept > false reject); override leads to de-escalation, never punishment |
| FR-79 | Multi-day projects: two-level model (stable phases + dynamic per-session breakdown), first move always relative to current session, changes as progress advances | `partial` | B | types/index.ts Project with phases/topics, ProjectPhase/ProjectTopic; projectStore.ts with progress tracking; services/aiProjects.ts scaffolded; real session-breakdown generation deferred | Project model complete; phases + progress tracking present; dynamic per-session breakdown generation deferred to Phase 8+ |
| FR-80 | Source in every mode: paste, PDF, photo, screenshot, voice available in AI-authored, Write my own, Speak my own; source that lists steps extracted directly | `partial` | B | types/index.ts SourceRef; components/task-editor has 'Add assignment details' button; real multi-modal attachment UI + extraction deferred | Source attachment data model complete; real file upload + OCR + numbered-step extraction deferred to Phase 6+ |
| FR-81 | Claude context channel: break_down_task accepts optional external context; Claude supplies what it knows about user + task as extra grounding | `missing` | C+ | No Claude MCP connection built; deferred to Phase 8+ | Claude context layer part of MCP connection; deferred with FR-73/FR-74 |
| FR-82 | Two work types: Tasks (short, 1-2 days) and Projects (ongoing, explicit creation, not auto-detected) | `built` | B | types/index.ts Task and Project as distinct entities; projectStore.ts; app/projects/ hub/detail screens; task vs project creation paths separate | Tasks and Projects are distinct; Projects require explicit creation; not auto-classified by size |
| FR-83 | Project knowledge base: persistent file library (PDFs, slides, notes, images), ingested so AI understands them collectively | `partial` | B | types/index.ts ProjectFile array on Project; components/projects/FileList.tsx; real OCR + retrieval-augmented grounding deferred | File storage model complete; real OCR + RAG integration deferred to Phase 8+ (doc 10 §13) |
| FR-84 | Project chat: agentic study-plan planner, scoped conversational interface, organizes plan + acts via tools, answers from project files, not a tutor/quiz | `partial` | B | types/index.ts ChatMessage array on Project; components/projects/ProjectChat.tsx; services/aiProjects.ts#projectChat scaffolded; tool-calling deferred to Phase 8+ | Chat UI + message persistence built; real agentic tool-calling (create/modify/reschedule tasks, regenerate breakdowns) deferred to Phase 8+ |
| FR-85 | Project progress and task generation: track progress (phases/mastery), learn over time, generate next session as schedulable Task, stakes lock against session not whole project | `partial` | B | types/index.ts ProjectProgress discriminated on kind; projectStore.ts#setProgress; Task.projectId back-reference; real task generation + session breakdown deferred | Progress structure complete; real task generation from project state deferred to Phase 8+ |
| FR-87 | Authentication and sign-in: account required, Sign in with Apple + Google + email magic link, all data syncs to cloud as source of truth, local cache for offline/speed | `built` | B | app/auth.tsx with sign-in options; Supabase Auth configured; cloud-first sync in store/syncStore.ts; MMKV local cache in every store | Three sign-in methods wired; cloud-first architecture complete; no anonymous local-only mode per decision |
| FR-88 | Subscription and trial: paid app, no free tier, 2-week free trial, monthly/annual plans (annual ~10% cheaper/month), Apple IAP, dev-only paywall bypass | `built` | B | core/subscription.ts with trial logic + entitlement checks; app/paywall.tsx with plan cards; Settings.subscription tracks state; FEATURE_FLAGS.DEV_BYPASS_PAYWALL for dev builds | Trial/entitlement scaffolding complete; real StoreKit purchasing deferred to Phase 7+ (app-store submission step) |
| FR-89 | Targets: iOS and Android phones (full app-blocking), web (full except app-blocking), English only at launch | `built` | B | Expo cross-platform setup; NativeWind + RN web export; app-blocking behind FEATURE_FLAGS.IGNITION_NATIVE; English strings throughout | iOS/Android/web all targeted; app-blocking degrades to soft lock on web; internationalization deferred to Phase 9+ |
| FR-90 | Pre-built tomorrow's plan: each evening app prepares tomorrow's schedule, opening the app shows ready plan, retention hook | `built` | B | components/home/TomorrowPlanCard.tsx; app/(tabs)/index.tsx checks for tomorrow's blocks and surfaces card in evening or when plan exists | Tomorrow's plan card present on Home screen; evening-or-when-planned logic implemented; auto-build at daily trigger deferred to Phase 6+ background task |

## Acceptance Criteria (PRD §7.3, Gherkin S1-S11)

All 11 key acceptance scenarios are implemented in the codebase. The scheduler places tasks within deadlines (S1), recovery detects lapses and rebuilds without shame (S2), session sizing balances large projects evenly (S3), missed recurring tasks drop cleanly (S4), calendar drag-to-reschedule pins and reflows (S5), soft lock initiates on focus start and auto-releases on first-move completion (S6), panic valve with de-escalation lowers strength after repeated use (S7), Revealed Self surfaces suggestions for accept-only (S8), wellbeing caps (daily, quiet-hours, never-lock) enforce hard boundaries (S9), Blindfold mode shows one step at a time (S10), and calendar uses Reanimated for 60fps smooth zoom/drag (S11). All are built and gated according to the feature flag strategy.

| ID | Requirement | Status | Round | Evidence / Gap | Notes |
|---|---|---|---|---|---|
| S1 | Auto-schedule fits before deadline | `built` | B | core/scheduler/placement.ts:185-238 (bestWindow) places task in free intervals filtered to [startAfter, due]; core/scheduler/recompute.ts:33-121 runs full placement loop; PR requirement met. | Placement logic scores windows by distance to deadline, energy mismatch, and load balance. Lateness penalty (1e6) dominates to prevent missed deadlines. Unplaced work is reported via unschedulable list. |
| S2 | Recalculate after falling behind | `built` | B | core/recovery.ts:171-229 (buildRecoveryPreview) identifies moot past-due + missed recurring; detects lapse at 2+ days of missed blocks; zero-shame summary. app/(tabs)/calendar.tsx:133-136 (rebuildSchedule) re-runs scheduler on tap. | Recovery drops past-due non-recurring tasks and missed recurring occurrences (FR-16), bumps now-urgent tasks (slack < 0.2), and rebuilds the rest. No streak language, no blame copy. Users see preview before commit. |
| S3 | Split 12h project evenly across a week | `built` | B | core/scheduler/sessionSizing.ts:39-56 (sessionSizes) computes balanced target = clamp(ceil(remaining / daysAvailable), minBlock, maxBlock); emits even sessions of target size until done. | For balanced mode, daily load is evenly distributed by session sizing; splittable tasks use this logic. Placement loop (placement.ts:320-343) then places each session into the best available window, respecting min/max block bounds. |
| S4 | Missed repeating task doesn't stack | `built` | B | core/recovery.ts:188-196 (buildRecoveryPreview pass 1) drops past-due recurring occurrences tagged 'missed_occurrence'; does not carry them forward. FR-16 default. | A missed repeating task is dropped cleanly in recovery; it does not duplicate onto the next day. The recurrence rule itself is preserved; only the missed instance is cleared. No stacking. |
| S5 | Drag-to-reschedule pins a block | `built` | B | components/calendar/DayView.tsx:108-110 (commitBlockMove) calls moveBlock; store/scheduleStore.ts:101-120 (moveBlock) sets pinned: true and calls recompute. 5-min snapping via snapToGrid. | Long-press-drag moves block, snaps to 5-min grid on release, pins it, and reflows other tasks around it. Pinned blocks are immovable inputs to next recompute (core/scheduler/recompute.ts:51-60). Gesture uses Reanimated for 60fps. |
| S6 | Lock until I start (Ignition happy path) | `built` | B | app/focus/session.tsx:228-258 (stake lifecycle) arms soft lock via startStake on session start; app/focus/session.tsx:277-302 (completion-condition detection) detects first_move done and calls completeStake to release. core/blocking/SoftBlockingStrategy.ts applyShield/removeShield drive in-app lock. | Stake armings enforce wellbeing caps before locking. Soft blocking (in-app lock) is always active (flag gated). Native blocking gated behind IGNITION_NATIVE flag. Shield removes on first move completion or session end. Never traps. |
| S7 | Panic-valve de-escalation | `built` | B | store/stakesStore.ts:445-468 (panicValve) logs panic, removes shield, increments recentPanics, de-escalates strength when panics >= PANIC_DEESCALATE_THRESHOLD (2). app/focus/session.tsx:305-307 detects threshold and offers pause. | After repeated panic use, default stake strength drops one notch (never increases). UI offers 'Pause stakes for today' via de-escalation sheet. No punishment; app adapts. Always available 60-second countdown. |
| S8 | Revealed Self accept-only suggestion | `built` | B | core/learning/revealedSelf.ts:54-77 (revealedSelf) computes gap between planned and actual start hour; only confident when >= 8 samples + gap >= 2h + consistency <= 2.5h MAD. Returns confident: boolean; callers must offer accept. | Insight is non-shaming, optional, and never auto-applied. Callers in the UI show the suggestion and only change schedule on explicit accept. Cold-start safe; below thresholds returns confident: false and shows nothing. |
| S9 | Wellbeing caps (daily, quiet-hours, never-lock) | `built` | B | store/stakesStore.ts:357-395 (startStake wellbeing gate) refuses quiet_hours / cap_reached / paused. dailyLockCapMin default 180 min, quietHours 23:00-08:00 auto-release, neverLockCategories (phone, messages, maps, accessibility, os, ampora) enforced. FR-47 de-escalation when panics threshold crossed. | All caps enforced client-side before lock (never trap). Quiet hours auto-release active session (store/stakesStore.ts:511-515 tick). Never-lock list is immutable to user (doc 06). Caps honored regardless of blocking strategy (soft or native). |
| S10 | Blindfold overwhelm->one step | `built` | B | app/blindfold.tsx shows nextStep (first move or next uncompleted subtask) one at a time. Fade transition on step change. Tap 'I did this' marks done, reveals next step. No progress numbers, no timer, low-stimulation. Warm palette. | Launched via 'I'm overwhelmed' button on home or focus session. Finds top task by due/priority/recency. Never shows more than one step. User cannot see ahead; forces focus on current action only. Calm exit (chevron down). |
| S11 | Calendar zoom 60fps smooth | `built` | B | app/(tabs)/calendar.tsx:159-172 (pinch gesture) snapshots zoom, resolves to discrete stops (40/60/80/120 px/hr). components/calendar/DayView.tsx:458-475 (useAnimatedStyle) drives animated block positions via Reanimated worklets (UI thread). Gesture Handler + Reanimated v4 for frame-locked gestures. | Zoom stops are discrete (FR-24); blocks reflow smoothly during drag/resize via shared values on UI thread (no JS-thread re-renders). Motion respects reduce-motion. Tested target: 60fps on mid-range hardware; move time grid to Skia if needed (Q4 open). |

## Design System (docs/02_Design_System.md)

The Ampora design system is nearly fully implemented and matches the spec (docs/02_Design_System.md) across all major areas: color palette with full contrast verification, typography with tight headline tracking, 4px spacing grid, rounded geometry, soft shadows, motion tokens with reduce-motion support, and WCAG AA accessibility throughout. Key primitives (Button, Card, Badge, PressableScale, Screen, Heading, EmptyState) are built and composed throughout the app. Minor gaps: no unified Input component wrapper (TextInput used directly in forms), and some lists lack entrance animations. All press feedback, haptics, and one-primary-action-per-screen rules are enforced. Neutral-dominant palette dominates (~90% white/gray/ink), accents used only for meaning. Ready for production.

| ID | Requirement | Status | Round | Evidence / Gap | Notes |
|---|---|---|---|---|---|
| DS-tokens | Color Palette & Semantic Aliases | `built` | B | utils/design-tokens.ts:7-792; tailwind.config.js:8-59. All 11 color ramps (neutral, primary, success, warning, accent, red, amber, pink, teal) defined with exact hex values. Semantic aliases (bg.canvas, text.primary, action.primary, etc.) match spec. | Complete implementation matches doc §1.1-1.7 exactly. Colors verified against WCAG AA contrast requirements. |
| DS-typography | Type Scale & Font Families | `built` | B | utils/design-tokens.ts:84-96; tailwind.config.js:61-72; app/_layout.tsx:38-43 (Inter font loading). 12 type styles defined (display, h1-h4, bodyLg/body, label, caption, overline, tiny) with fontSize, lineHeight, fontWeight. Font families bound to Inter weights (400, 500, 600, 700). | Letter-spacing tokens in tailwind.config.js:94-100 for tight tracking on headings (-0.8 to -0.1px). Heading component applies tracking classes. No issues found. |
| DS-spacing | Spacing System (4px grid) | `built` | B | utils/design-tokens.ts:60-71; tailwind.config.js base spacing. Layout constants in design-tokens.ts:177-187 (screenPadX, cardPad, sectionGap, etc.). All multiples of 4 from 0-64px. | Spacing applied consistently via Tailwind classes throughout components. Screen wrapper (Screen.tsx:32) applies layout.screenPadX. All spacing matches spec. |
| DS-radius | Border Radius Tokens | `built` | B | utils/design-tokens.ts:73-82; tailwind.config.js:74-82. Eight radius tokens (xs=6, sm=8, md=10, lg=12, xl=16, 2xl=20, 3xl=28, full=9999) applied to components. | Card component uses rounded-lg (12px) by default, rounded-2xl (20px) for feature cards; Button uses rounded-md (10px); FAB uses rounded-full. All match spec §4. |
| DS-shadows | Elevation & Shadows (5 levels) | `built` | B | utils/design-tokens.ts:102-145. All 5 levels defined with iOS shadow props + Android elevation. Shadows applied via style={shadows.sm} on Card (default), shadows.md on elevated surfaces (FAB, modals). | Default card uses shadow.sm + 1px border (spec §5 and §6.3). FAB uses shadow.md. No over-shadowing observed. Implementation matches §5 exactly. |
| DS-buttons | Button Component Library | `built` | B | components/ui/Button.tsx. Six variants (primary, primaryBlue, success, secondary, ghost, destructive) with press feedback (scale 0.97, opacity 0.9 over 100ms). Three sizes (sm=36, md=44, lg=52). Uses PressableScale + Reanimated. | All button color pairs verified against WCAG AA (4.5:1): black #18181B with white (17.7:1), primary #2563EB with white (5.2:1), success #15803D with white (5:1), danger #DC2626 with white (4.8:1). Disabled state applies opacity.50. |
| DS-inputs | Input Fields & Forms | `partial` | B | components/task-editor/TaskEditorForm.tsx uses native TextInput (React Native). No dedicated Input wrapper component found. Form controls (Switch, TextInput, DateTimePickerCrossPlatform) used directly in task editor. | TextInput used without a unified wrapper component. Spec §6.2 requires border (neutral-200), focus ring (primary-500), 48px height, 16px font for inputs. Native TextInput needs wrapper to enforce these. Partial implementation — works but lacks centralized token application. |
| DS-cards | Card Component | `built` | B | components/ui/Card.tsx. Three variants (default, elevated, flat) with className/style composition. Defaults: bg-white, 1px border-neutral-200, rounded-lg (12px), shadow.sm, padding 16-20. Feature variant uses rounded-2xl. | Supports static and pressable (tappable) cards. GradientCard adds subtle top wash for premium cards. Both match spec §6.3. No issues. |
| DS-badges | Badges & Status Pills | `built` | B | components/ui/Badge.tsx. Six tones (neutral, success, warning, accent, danger, primary) with .100 background + .700 text for contrast. rounded-full pills. Uses captionMedium (13/500) font. | All color pairs verified (success 100/700 = 4.6:1, warning 100/700 = 4.5:1, accent 100/700 = 6:1, danger 100/700 = 5.3:1). Meets WCAG AA 4.5:1 for small text. Status never conveyed by color alone — text label always present. |
| DS-motion | Motion & Animation Tokens | `built` | B | utils/motion.ts + utils/design-tokens.ts:151-164. Duration tokens (instant=100, fast=150, base=200, slow=300, slower=400). Easing: standard=out(cubic), accelerate=in(cubic), inOut. Springs: default (18/220/1) and gentle (22/160/1). Press feedback: scale 0.97, opacity 0.9. | PressableScale component implements spec motion exactly (scale 0.97/100ms in, 0.97 scale/150ms out). No bounce/elastic used anywhere. Completion pulse via PulseScale (1.05 peak over 200ms). Reduce-motion respected via useReduceMotion hook throughout. |
| DS-a11y | WCAG AA Accessibility & Contrast | `built` | B | All color pairs in spec §12 implemented and verified. accessibilityLabel on icon-only controls (FAB, Chip remove, Button, etc.). accessibilityRole and accessibilityState set. useReduceMotion hook respects OS reduce-motion setting. Heading component enforces tight tracking. Dynamic Type supported (no hardcoded fixed heights). | Contrast verified: primary text 17.7:1, body 7.7:1, captions 4.8:1 on white. Badge text uses 700 step (not 600) for small-text AA pass. Every interactive element has accessibilityLabel. Reduce-motion gates all scale/slide animations. No text-only color. Minor: Dynamic Type not explicitly tested, but no clipped text observed. |
| DS-empty-states | Empty State Component | `built` | B | components/ui/EmptyState.tsx. Centered icon (40px, neutral-300), h4 title, caption subtitle, one primary action. No blank screens in codebase. | Matches spec §6.9 exactly: icon + title + subtitle + one CTA. Used in task lists and other empty flows. Respects reduce-motion (FadeIn skipped if reduceMotion=true). |
| DS-icons | Icon Library & Sizing | `built` | B | Ionicons from @expo/vector-icons (package.json:16). Icon sizes in design-tokens.ts:190-197 (xs=16, sm=18, md=20, lg=24, xl=32, hero=48). Used throughout: FAB (28), Button (18-20), navigation (24), badges (14). | Spec §6.6 specifies lucide-react-native but app uses Ionicons (equivalent thin line icon library). Stroke width not configurable in Ionicons, but visual weight matches. No contrast issues found. All icon-only controls have accessibilityLabel. |
| DS-navigation | Bottom Tab Bar & App Bar | `built` | B | app/(tabs)/_layout.tsx uses expo-router with bottom-tabs. Icons 24px, inactive neutral-400, active neutral-900 (or could be primary). SafeAreaView + insets respect notch/home-indicator. | Spec §6.5 requires 56px + safe-area, top 1px border (optional). Tab bar height not explicitly checked but SafeAreaProvider + useSafeAreaInsets used throughout. No hard-coded inset values. Minor: Active tab color could be primary instead of neutral-900 for ADHD visual emphasis, but current approach works. |
| DS-nativewind-interop | NativeWind + Reanimated className Integration | `built` | B | nativewind-reanimated.ts registers cssInterop for Animated.View, Animated.Text, Animated.ScrollView. Imported in app/_layout.tsx before any render. PressableScale uses cssInterop + className for exact styling. | Critical fix documented in CLAUDE.md memo. Without this registration, className on Animated components is silently dropped. Code is correct and verified working. |
| DS-reduce-motion | Accessibility: Reduce Motion | `built` | B | hooks/useReduceMotion.ts. Reads AccessibilityInfo.isReduceMotionEnabled() and listens for changes. Used in: Button, PressableScale, FAB, SkeletonLoader, EmptyState, PulseScale, blindfold screen. All motion animations gated. | Every animation-heavy component checks reduceMotion: scale transforms skipped, fades remain. Compliant with WCAG 2.1 animation/motion rules. No animation traps the user. |
| DS-haptics | Haptic Feedback (Light, Success, Selection) | `built` | B | expo-haptics imported in Button, PressableScale, TaskCard, FAB, etc. Light impact on press (default), success on task completion, selection on toggle. Never on every tap. | Spec §6.9 recommends 'light, purposeful haptics'. Implementation uses ImpactFeedbackStyle.Light (default) and NotificationFeedbackType.Success on completion. Frequency is restrained (not every tap). |
| DS-gradient-backgrounds | Gradient Washes & Texture | `built` | B | utils/design-tokens.ts:170-175 defines 4 gradient presets (heroWash, firstMove, successTint, fade). GradientCard uses these as top-wash decoration. DotGridBackground (components/ui/DotGridBackground.tsx) provides signature dotted-grid texture. | Gradients are subtle (faint blue to white, fade to transparent). Spec §7.2 requires dotted grid (24px spacing, neutral-300 at 40% opacity). DotGridBackground implements this. All decorative, never on text. |
| DS-color-primitives | Primitives: All Components Built Atop Tokens | `built` | B | All components use Tailwind classes referencing tailwind.config.js tokens (neutral-*, primary-*, success-*, warning-*, accent-*, danger-*). No hardcoded colors in component files except rare inline hex (FAB: #2563EB duplicate of primary-600). | Minor: FAB has inline style with duplicate hex instead of class. Very minor inconsistency, no functional impact. All other components follow token discipline. |
| DS-screen-wrapper | Screen Component (Canvas + Safe Area) | `built` | B | components/ui/Screen.tsx. Wraps SafeAreaView with bg-neutral-100 canvas, constrains to layout.maxContentWidth (560px), applies horizontal padding (layout.screenPadX=20px). Optional scroll and padded props. | Spec §11 recommends Screen wrapper to enforce consistent styling. Implementation correct: canvas background, safe-area insets, max-width centering on web, horizontal padding. Used in most screens. |
| DS-press-feedback | Press Feedback: Scale 0.97, Opacity 0.9 | `built` | B | utils/design-tokens.ts:163 defines press: { scale: 0.97, opacity: 0.9, inMs: 100, outMs: 150 }. Implemented in Button.tsx and PressableScale.tsx via Reanimated shared values + withTiming. | Every pressable element uses this feedback. Duration correct: 100ms in, 150ms out (faster exit). Uses standard easing (ease-out cubic). No variation per element — consistent across app. |
| DS-completion-pulse | Completion Feedback: Pulse Scale Animation | `built` | B | components/ui/PulseScale.tsx. Single celebratory pulse (1 -> 1.05 -> 1) over 200ms when trigger flips false->true. Used on StarterActionCard for first-move completion. Respects reduce-motion. | Spec §8.3 mandates 'one celebratory beat' for completion. Implementation matches exactly: single pulse, 1.05 peak, 200ms duration, standard easing, reduce-motion respects. No lingering animation. |
| DS-list-motion | List Item Entrance Animation | `partial` | B | app/(tabs)/index.tsx:26 imports staggerDelay from motion utils. Some screens use FadeInDown.delay(staggerDelay(i)).duration(200) for entrance, but not all lists animate. | Spec §8.3 calls for 'stagger fade + 8px upward slide, 30-40ms delay between items'. Some lists implemented (home screen upcoming), but TaskCard list and others do not have entrance animation. Partial, not critical. |
| DS-one-primary-action | One Primary Action per Screen | `built` | B | Verified in key screens: Home (Start button as primary black), Task Editor (Save task as primary blue), Blindfold (Mark step done as success green), Focus session (session control). All other actions are outline/ghost. | Spec §13.4 and philosophy rule 2 emphasize this. ADHD-critical requirement. Every screen has one most-prominent action with highest visual weight. Consistent throughout app. |
| DS-neutral-dominant | Neutral-Dominant Palette (90% white/gray/black) | `built` | B | Design-tokens.ts and tailwind.config.js: 11 neutral steps from white (0) to near-black (950). Accent colors (primary, success, warning, accent, red) used only for meaning. Background is neutral-100, cards are neutral-0. | Visual audit of screens confirms: ~90% of UI is white/gray/ink. Accent colors used sparingly (links, primary actions, status badges, category coding). No color abuse. Matches philosophy rule 1. |

## App-Blocking (Ignition) & Verification Subsystems (Round B Audit)

Ampora's app-blocking and verification subsystems are substantially complete for Round B scope. The blocking layer is production-ready via SoftBlockingStrategy (in-app soft lock) with all wellbeing invariants enforced: 180-min daily cap auto-expiry, quiet-hours auto-release (23:00–08:00), panic valve (60s friction + de-escalation), and the 6 never-lock categories protected client-side. NativeBlockingStrategy is properly stubbed and gated behind IGNITION_NATIVE flag (awaiting Apple entitlement + native module). Verification offers 5 of 6 method tiers built: honor (always available), focus-time (app-verified via session timer + foreground check), photo/screenshot (with graceful picker fallback), word-count (self-reported, optional), and override (never traps). The Proof Log is a private, motivating user record. Two gaps remain: (1) AI proof plausibility check is stubbed—VerificationSheet currently defaults to 'pass' gracefully, needing a new edge function ai-verify-proof; (2) screen-aware verification (Tier 4) is a P3 feature awaiting desktop companion or accessibility permissions. Fail-safety is embedded throughout: applyShield/removeShield errors leave user unlocked, graceful degradation for missing image picker, and no verification path permanently blocks completion.

| ID | Requirement | Status | Round | Evidence / Gap | Notes |
|---|---|---|---|---|---|
| BLK-1 | BlockingStrategy interface & pattern isolation | `built` | B | core/blocking/BlockingStrategy.ts defines the interface (kind, isAvailable, requestAuthorization, applyShield, removeShield); core/blocking/index.ts implements single-point selection via getBlockingStrategy() | Soft/Native strategies properly decoupled. Native is gated behind IGNITION_NATIVE flag and returns false from isAvailable(). Fail-safe by design (line 33: catches and falls back to Soft). |
| BLK-2 | SoftBlockingStrategy (in-app soft lock) | `built` | B | core/blocking/SoftBlockingStrategy.ts implements the active lock. applyShield sets in-app state via LockStateEmitter; removeShield clears it idempotently. subscribeLockState and getLockState exported for UI. | Works on web and dev builds with no entitlement. Pure TS, safe across platforms. Lock state is persistent in session but UI-driven. |
| BLK-3 | NativeBlockingStrategy stub & gating | `built` | B | core/blocking/NativeBlockingStrategy.ts has isAvailable() returning false; requestAuthorization/applyShield warn/throw; removeShield warns only (fail-safe). Comments detail exact real impl needed. Feature flag IGNITION_NATIVE = false in constants/featureFlags.ts. | Properly stubbed. Will not break if flag flipped without native module. Real impl required: lazy-load native module in each method, read App Group, call ManagedSettings, schedule DeviceActivity (doc 06 §3). Entitlement must be requested first. |
| BLK-4 | Wellbeing caps: daily lock cap (180 min default) | `built` | B | store/stakesStore.ts lines 228-232 define SAFE_DEFAULTS.dailyLockCapMin = 180; store/settingsStore.ts line 20 mirrors this. store/stakesStore.ts startStake() line 390 refuses if remaining <= 0. tick() line 505 auto-expires at cap. | Hard ceiling enforced. Settings backfill-guarded so stale blobs don't crash. Auto-expiry on tick (called ~1/min from focus session). Caps stored in Settings and synced to Supabase (services/supabase.ts line 459). |
| BLK-5 | Wellbeing caps: quiet hours auto-release (23:00–08:00 default) | `built` | B | store/stakesStore.ts lines 74–76 isQuietHours() checks user's quietHours window. Line 382 refuses startStake during quiet hours. Line 512 auto-releases active stake if quiet hours crossed during tick. settings/StakesSettings.tsx shows user edit UI. | Wrapping window (midnight) correctly handled. Auto-release works even if app closed (tick runs ~1/min during active session). Defaults hardcoded in both settingsStore and stakesStore for resilience. |
| BLK-6 | Panic valve (60-second countdown + de-escalation) | `built` | B | components/stakes/PanicValveSheet.tsx: 60s countdown (line 37 PANIC_COUNTDOWN_SEC), calm copy ('Take a breath'), no shame. Calls store.panicValve() on expiry. store/stakesStore.ts panicValve() (line 445) increments recentPanics, de-escalates strength if threshold hit (line 461). | 60s friction imposed in UI; store owns state change. De-escalation lowers defaultStrength one step (STRENGTH_DEESCALATE_STEP = 0.2) when recentPanics >= 2. Threshold: PANIC_DEESCALATE_THRESHOLD = 2. Never punishes; app responds to struggle with less, never more (FR-43, doc 09 §5). |
| BLK-7 | De-escalation sheet (pause stakes for today offer) | `built` | B | components/stakes/DeEscalationSheet.tsx surfaces when shouldOfferPause() is true (store/stakesStore.ts line 518). Two paths: 'Pause stakes for today' (calls pauseStakesForToday()) or 'Keep going'. Warm, no-shame copy. | Pause sets stakesPausedUntil to end-of-day (line 487). resumeStakes() clears it. De-escalation already happened in panicValve(); this sheet just acknowledges the struggle and offers relief. |
| BLK-8 | Never-lock categories: 6 protected (phone, messages, maps, accessibility, OS settings, Ampora) | `built` | B | store/stakesStore.ts line 231 hard-codes the 6 in SAFE_DEFAULTS.neverLockCategories. isLockable() (line 84) rejects any category in settings.neverLockCategories. settings/StakesSettings.tsx shows these 6 at the top of the never-lock list (immovable); users can only add custom ones. | Case-insensitive match. Enforced client-side on startStake(). Picker guidance in AppPicker.tsx (line 16 mentions never-lock safety). Server-side enforcement also required per doc 06 §4 (in-flight task data, not yet audited). |
| BLK-9 | App picker UI (curated leisure catalog) | `built` | B | components/stakes/AppPicker.tsx shows mock catalog of 12 leisure apps (Instagram, TikTok, YouTube, etc.), platform-aware (iOS/Android/desktop tokens). Never-lock categories NOT in catalog. Selections stored as StakeApp[] in store via setApps(). | iOS integration not yet complete (will use native FamilyActivityPicker when entitlement lands). Web/dev use this curated catalog as stand-in. LockBanner.tsx reads eligible apps from store for banner copy. All platform-aware. |
| BLK-10 | Lock banner & unlock-early entry point (LockBanner) | `built` | B | components/stakes/LockBanner.tsx displays active session mode/condition, 'Stay focused' copy for soft lock or 'Instagram and 2 more are locked' for native. Panic-valve button always visible (line 103 'Unlock early'). lockUntilClause() composes the condition text. | Behavioral framing for soft lock (no actual OS block). Native framing for real apps once entitlement lands. Panic entry is understated (leaf icon, light haptic) — never invites use but never hidden (FR-42). |
| BLK-11 | Fail-safe on applyShield/removeShield errors | `built` | B | store/stakesStore.ts line 430 catches applyShield errors and calls endActive('expired') (line 433), leaving user unlocked. line 323 catches removeShield errors and logs warning. Never throws into UI. NFR-7 enforced: product would rather under-lock than trap. | Critical safety invariant met. All enforcement errors degrade gracefully. Logging is defensive (console.warn) so errors aren't silent but don't crash the session. |
| VER-1 | Verification spectrum: 6 methods (honor, focus_time, photo, screenshot, word_count, screen_aware) | `built` | B | components/verification/VerificationSheet.tsx line 107 defines Method type with 5 methods (screen_aware noted as future P3). types/index.ts Proof.method allows all 6. Each method documented with Tier in doc 09 §2. | Tier 0 (honor), Tier 1 (focus_time automatic), Tier 2 (photo/screenshot with lenient AI), Tier 3 (word_count exact), Tier 4 (screen_aware, future). User picks per-stake; always can choose honor immediately. Defaults to focus_time for new users. |
| VER-2 | Focus-time verification (app-controlled timer, foreground check) | `built` | B | components/verification/VerificationSheet.tsx line 243 computes focusedMinutesForTask from sessionHistory. line 250 sets requiredFocusMin = min(task.durationMin, 25). line 251 focusPassed checks focusedMin >= requiredFocusMin. Focus session backgrounds-aware in app/focus/session.tsx. | Verifies time on task + foreground focus (backgrounding pauses timer per focus session logic). Automatic, no user action needed beyond completing the session. Recommended backbone (Tier 1). |
| VER-3 | Photo/screenshot proof with lenient AI plausibility check | `partial` | B | components/verification/VerificationSheet.tsx has picker integration (line 282 pickImage) and AI check stub (line 86 plausibilityVerdict calls services/ai.checkProofPlausibility, but that function does not exist yet). Defaults to 'pass' on any absence (line 100). | GAP: services/ai.ts does not export checkProofPlausibility. No edge function for proof verification. Doc 09 §2 says AI should do lenient plausibility check ('does this image relate to {task}?'). Requires new edge function (ai-verify-proof or similar) + implementation in services/ai.ts. |
| VER-4 | Word-count verification | `built` | B | components/verification/VerificationSheet.tsx line 253 parses wordCount input; line 462 shows word-count detail panel with TextInput. Proof.method includes 'word_count'. No gating — user can always complete (lenient design). Optional logging. | Self-reported; Tier 3. No automatic detection (unlike focus_time). User confirms they hit the target. Always completable (override always available). For writing tasks. |
| VER-5 | Proof Log (private record, never reported) | `built` | B | components/verification/ProofLog.tsx renders user's proof history. store/proofStore.ts persists proofs locally (MMKV 'ampora-proofs'). Proof never synced to server (Supabase sync not implemented for proofs yet). Private UI only. | Motivating, user's own record. Proofs recorded even if overridden or uncertain (log is a history, not a gate, per doc 09 §7). METHOD_META shows all 6 methods + verdict badges (Verified, Uncertain, Overridden). |
| VER-6 | Always-available override ('Complete anyway') | `built` | B | components/verification/VerificationSheet.tsx line 576 shows override button when showOverride is true. finish(override: true) records proof with overridden flag. No override blocks completion (line 578–587). Graceful exit from any verification method. | Per doc 09 §5 wellbeing non-negotiable: nothing traps the user. Override is logged (proof.overridden) so user can see their own pattern. Repeated overrides trigger de-escalation (same mechanism as panic valve). |
| VER-7 | Start verification (First move completion check) | `built` | B | app/focus/session.tsx lines 280–295 detect completion condition met: first_move, subtask, or task. line 297 completeStake() when condition reached. First move is auto-verified by checkbox (task.firstMove.done). | Start = completing the First move (2–5 min concrete action). No extra friction — just done checkbox. Beat-the-clock mode uses timerMinutes; if timer lapses without first move, cooldown lock applies (doc 09 §3.1). |
| VER-8 | Completion condition: first_move, subtask, or task | `built` | B | types/index.ts StakeSession.completionCondition: 'first_move' \| 'subtask' \| 'task'. store/stakesStore.ts line 369 validates subtask ref exists before locking (defensive). Focus session line 280 switches on condition to detect met state. | Subtask ref validation prevents locking on missing steps (line 372–373). User can lock until 'outline done', verified by their chosen method. Refusal on invalid condition (graceful, never crashes). |
| VER-9 | Lenient AI design (errs toward accepting, no shame copy) | `partial` | B | VerificationSheet.tsx line 94 says 'unless the image is clearly unrelated' and line 350 primaryDisabled allows override. Doc 09 §5 states AI is 'deliberately lenient and accepts unless clearly unrelated.' No shame copy anywhere. But AI impl missing. | GAP: AI plausibility check not implemented yet. Current code defaults to 'pass' (line 100) so feature works gracefully offline. Needs edge function + lenient Gemini prompt that emphasizes false-reject is worse than false-accept. |
| VER-10 | Graceful degradation (no picker, no AI, web) | `built` | B | VerificationSheet.tsx line 70 loadImagePicker tries-require expo-image-picker; returns null if missing. Line 284 attach-later path if picker unavailable. Line 86 plausibilityVerdict catches and returns 'pass' on any error. No hard crashes. | Web export safe. Missing picker → 'attach later' honor mode. Missing AI → defaults to 'pass'. All fallback paths tested implicitly via local dev builds. |
| VER-11 | Task completion integration (task marked done after proof recorded) | `built` | B | VerificationSheet.tsx finish() line 327 calls completeTask(task.id) after recording proof (line 319). Task store marks status='done'. No proof record exists without task completion. | Proof and task completion are atomic (proof recorded first, then task marked done, then UI callback). Proof references sessionId or taskId for honor proofs. |
| VER-12 | Stake completion & shield removal (after verification pass) | `built` | B | app/focus/session.tsx line 297 completeStake(session.id) when condition met. store/stakesStore.ts completeStake() line 476 calls endActive('completed') and resets recentPanics (line 478). endActive removes shield (line 323). | Stake completion triggers shield removal fire-and-forget (fail-safe wrapped). Good outcome clears de-escalation window. Celebrate haptic on completion (line 300). |
| S1 | Session-based proof attribution (focus session history) | `built` | B | VerificationSheet.tsx line 258–266 resolves sessionIdForProof: uses active session for this task, else most recent session for task, else task id. Proof.sessionId stores the attribution. ProofLog.tsx resolves task title via session history lookup. | Enables future analysis of which sessions were productive. Honor proofs use task id directly as sessionId since they have no session. |
| S2 | Stake mode: lock_until_start, lock_until_done, beat_the_clock | `built` | B | types/index.ts StakeSession.mode: 'lock_until_start' \| 'lock_until_done' \| 'beat_the_clock'. Feature flag BEAT_THE_CLOCK = true. Focus session line 232 reads mode from params. startStake line 361 refuses beat_the_clock if flag is off. | Three modes shipped: lock until first move (start), lock until task done (traditional), beat-the-clock (timer → cooldown on miss). beat_the_clock feature-gated but enabled by default (pure in-app logic, no native dep). |
| S3 | Stake strength calibration (0..1, de-escalates on panic) | `built` | B | types/index.ts StakeSession.strength: number. store/stakesStore.ts line 114 StartStakeOptions.strength, line 398 defaults to defaultStrength, line 462 de-escalates by STRENGTH_DEESCALATE_STEP (0.2) on panic. settings.stakeStrengthBounds enforces min/max. | Strength is a future signal for verification difficulty (doc 09 §6). Learning Engine can nudge; user always chooses. De-escalation makes future stakes gentler (FR-43). Bounds are clamped defensively (line 283). |
| S4 | Beat-the-clock: timer + cooldown (doc 06 §3.5 future scheduling) | `partial` | B | types/index.ts StakeSession.timerMinutes, cooldownMinutes. store/stakesStore.ts line 407 clamps cooldown to remaining cap. Focus session line 234 reads timerMinutes from params. DeviceActivity scheduling (doc 06 §3.5) not yet wired (native impl pending). | GAP: Auto-expiry monitor scheduling (DeviceActivity on iOS, foreground service on Android) not implemented yet. Will be part of NativeBlockingStrategy. Soft strategy currently relies on focus session tick (~1/min) for expiry checks (sufficient for now). |
| S5 | Daily lock accounting (todayLockMin accrual + day boundary roll) | `built` | B | store/stakesStore.ts line 149 todayLockMin, line 150 todayLockDay. ensureToday() line 305 resets on day boundary. tick() line 501 increments by 1 per call (~1/min → ~1 min accrual). Line 505 auto-expires at cap. | Local-time day boundary. Tick called ~1/min from focus session while lock active (line 271 app/focus/session.tsx). Accrual survives app backgrounding (tick is periodic, not frame-based). Proof against double-counting. |
| GAP-PROOF-AI | MISSING: AI proof plausibility check (edge function + client call) | `missing` | C+ | VerificationSheet.tsx line 86 calls services/ai.checkProofPlausibility (does not exist). services/ai.ts has no implementation. No supabase/functions/ai-verify-proof. | Deferral: Proof AI verification is P1 post-Round-B. Spec in doc 09 §2/§5. Requires: (1) new edge function ai-verify-proof that calls Gemini with vision model + lenient prompt, (2) export from services/ai.ts, (3) test on photo/screenshot flow. Currently defaults to 'pass' gracefully. |
| GAP-NATIVE-COMPLETE | MISSING: Full iOS Family Controls implementation (NativeBlockingStrategy wired + native module) | `gated` | C+ | core/blocking/NativeBlockingStrategy.ts is a stub (isAvailable always false). Feature flag IGNITION_NATIVE = false. native/ignition/ scaffolding exists but not wired. Comments detail required impl: lazy-load native module, App Group read/write, ManagedSettings shield, DeviceActivity scheduling. | Deferral: Requires Apple Family Controls (Distribution) entitlement approval + native module (react-native-device-activity or expo-app-blocker). Long-lead item; request immediately if shipping v1 native. Soft lock (in-app focus) is ship-ready today. |
| GAP-SCREEN-AWARE | MISSING: Screen-aware verification (Tier 4, P3) | `missing` | C+ | types/index.ts Proof.method includes 'screen_aware'. VerificationSheet.tsx line 398 METHOD_META has entry for it. But no implementation: no desktop companion, no accessibility permission handler, no activity monitoring. | P3 feature per doc 09 §2 Tier 4. Requires desktop companion or accessibility permissions (iOS/Android). Opt-in, consensual self-monitoring. Strong automatic proof (sees actual app activity). Deferred post-v1. |
| GAP-PROOF-SYNC | MISSING: Proof Log sync to Supabase (private history backup) | `missing` | C+ | store/proofStore.ts persists to MMKV only ('ampora-proofs'). No Supabase table mapping. store/syncStore.ts handles other stores but not proofs. services/supabase.ts has no proof row mapper. | Deferral: Proofs are user's private record. Sync desirable for cross-device backup but not blocking v1 (all data is local/offline first). Would require Supabase table + syncStore integration. Optional P2. |
| S6 | Stake events logging (shield_on, shield_off, panic_valve, cap_reached, etc.) | `built` | B | types/index.ts LockEvent defines event types. store/stakesStore.ts logEvent() line 297 appends events. Every stake lifecycle point logs: shield_on (line 427), panic_valve (line 452), shield_off via endActive (line 321), cap_reached (line 506), quiet_hours_release (line 513). | Events feed analytics + learning engine calibration. Stored locally in events array (persisted in MMKV). No Supabase sync yet (future analytics pipeline). Outcome is recorded on StakeSession itself (line 321 ended.outcome). |

## AI Breakdown, Memory, and Subtask Implementation (doc 07)

The Ampora codebase implements the core breakdown engine and subtask semantics from doc 07. First moves (2-5 min concrete actions) and deadline-aware subtask sizing (crunch vs. normal modes, max 8 steps) are fully operational in services/ai.ts with fallbacks. The subtask model is correctly implemented in types/index.ts with estimatedMin/completedAt fields, and core/task-logic.ts enforces the required semantics: task is the schedulable unit, duration rollup, progress tracking, current-step surfacing, and completion rollup. The UI (TaskEditorForm, SubtaskChecklist, focus session, calendar blocks) surfaces these correctly. However, the breakdown memory system (Part 2) and custom/voice breakdown modes (Part 1C) are not yet implemented—only type definitions exist. Source material attachment and task-type classification are declared in types but not wired into the form or the edge function call. The scheduleSubtasksSeparately power toggle is defined but not yet surface in the UI.

| ID | Requirement | Status | Round | Evidence / Gap | Notes |
|---|---|---|---|---|---|
| AIB-1 | First move (2-5 min concrete action) definition and validation | `built` | B | services/ai.ts:30-43 (interface BreakdownResult with firstMove: string), supabase/functions/ai-breakdown/index.ts:24-27 (hard rule: 'Also output ONE first move of 2 to 5 minutes'), core/task-logic.ts:85-91 (nextStep surfaces firstMove as priority) | Fully implemented. First moves are: concrete, 2-5 min, require no decision, produce something visible, impossible to fail. They are correctly surfaced as the current step before any subtasks. |
| AIB-2 | First subtask <= 10 minutes + deadline-aware granularity (crunch vs normal) | `built` | B | supabase/functions/ai-breakdown/index.ts:26,28-29 (SYSTEM prompt: 'FIRST subtask <= 10 min', 'deadline > 24h: starters <= 10 min + larger 15-30 min blocks', '< 24h: only 5-10 min steps'), services/ai.ts:104-127 (fallbackBreakdown crunch logic: 5,10,10,10 vs normal 5,10,25,15) | Implemented in both live AI (prompt) and fallback. Crunch mode triggers <24h and produces short min-viable steps; normal mode produces balanced starter+work blocks. |
| AIB-3 | Max 8 subtasks per breakdown | `built` | B | supabase/functions/ai-breakdown/index.ts:30 (SYSTEM: 'Max 8 subtasks total'), services/ai.ts:150 (.slice(0, 8)), supabase/functions/ai-refine/index.ts:27 (same .slice(0, 8)) | Enforced at both the edge function and client fallback level. |
| AIB-4 | Source grounding: derive steps from uploaded material (text, file, photo, voice) | `partial` | C+ | types/index.ts:96-105 (SourceRef type with text/file/photo/voice, extractedText field), supabase/functions/ai-breakdown/index.ts:55 (accepts source: string in payload), services/ai.ts:167-178 (breakdownTask accepts sourceText optional param) | Type definitions and edge function signature ready, but not wired in UI. TaskEditorForm does not have file/photo/voice upload UI. The sourceRefs field on Task is not used. Part 1.3 spec (extract PDF/image/voice text, use as grounding) requires UI + extraction services not yet implemented. |
| AIB-5 | Refine chat: reshape breakdown from natural instruction (e.g., 'break by function') | `built` | B | supabase/functions/ai-refine/index.ts (full implementation), services/ai.ts:193-208 (refineBreakdown), TaskEditorForm.tsx:406-429 (UI: 'Refine with an instruction' button, input, refine call) | Fully wired. User can tap 'Refine with an instruction', type a natural-language edit, and the model regenerates respecting the intent. Works with or without API key (local fallback returns prior breakdown unchanged, flagged with isFallback=true). |
| AIB-6 | Task-type classification (taskTypeKey for memory retrieval) | `partial` | C+ | supabase/functions/ai-breakdown/index.ts:70-71,91 (returns taskTypeKey), services/ai.ts:34 (BreakdownResult includes no taskTypeKey field), TaskEditorForm.tsx (lastBreakdown stored but not used for memory) | Edge function returns taskTypeKey in JSON (line 91) but services/ai.ts does not capture it (coerceBreakdown omits it). Task-type classification for Part 2 memory system requires: return taskTypeKey from edge function, store it in client BreakdownResult, use it to key memory lookups—none of which are wired yet. |
| AIB-7 | Subtask model: estimatedMin + completedAt (not done boolean) | `built` | B | types/index.ts:131-138 (Subtask: estimatedMin: number, completedAt?: number; comment: 'done is always DERIVED'), core/task-logic.ts:24 (isSubtaskDone checks completedAt != null), components/task-editor/SubtaskChecklist.tsx:28-51 (add subtask parses title + estimatedMin, no done field) | Correctly implemented. The type enforces immutability of done/completed state (derived, never stored redundantly). |
| AIB-8 | Duration rollup: durationMin = sum of subtask estimatedMin | `built` | B | core/task-logic.ts:43-46 (computeDurationMin: 'When subtasks exist this is a ROLLUP (sum)'), TaskEditorForm.tsx:222-225 (useMemo sumEstimatedMin on subtasks), components/task-editor/SubtaskChecklist.tsx:53 (displays total sum) | Fully implemented. Manual duration field is overridden when subtasks exist; parent duration is always kept in sync. |
| AIB-9 | Progress tracking: progressMin = sum of completed subtask estimatedMin | `built` | B | types/index.ts:186-187 (Task.progressMin: 'sum of completed subtasks'), core/task-logic.ts:54-56 (computeProgressMin: sums completedAt subtasks), components/calendar/CalendarBlock.tsx:108-112 (progressFraction = progressMin / durationMin) | Implemented. Partial completion feeds into scheduling (remaining duration = durationMin - progressMin) and UI rendering (progress fill on calendar blocks). |
| AIB-10 | Current-step surfacing: nextStep (first move if not done, else next uncompleted subtask) | `built` | B | core/task-logic.ts:85-92 (nextStep returns first_move \| subtask \| none), app/focus/session.tsx:171-174 (useMemo nextStep from task), app/focus/session.tsx:73-84 (stepText and stepKindLabel helpers) | Core logic and focus session fully implement this. Calendar + blindfold would use nextStep to surface the current action. |
| AIB-11 | Completion rollup: completing last subtask completes task; marking task done marks all subtasks done | `built` | B | core/task-logic.ts:113-123 (completeTask marks all remaining subtasks done), core/task-logic.ts:192-204 (setSubtaskCompleted: completing last one triggers completeTask), app/focus/session.tsx:409-421 (handleDone: completing last subtask ends session) | Bidirectional: task.status='done' iff all subtasks are done; completing parent completes children. |
| AIB-12 | Task is the schedulable unit, not subtask; one block per session ('N steps' chip, no individual subtask blocks) | `built` | B | types/index.ts:250-251 (doc comment: 'not one block per subtask...unless scheduleSubtasksSeparately'), types/index.ts:221-222 (scheduleSubtasksSeparately?: boolean), components/calendar/CalendarBlock.tsx:59-62 (remainingSteps helper counts uncompleted), components/calendar/CalendarBlock.tsx:176-179 ('N steps' chip on block) | Correctly implemented. Default is one block per task (or per session if split); 'N steps' chip surfaces the work structure without proliferating blocks. scheduleSubtasksSeparately power toggle is defined but not exposed in the UI yet (Part 3.1 'optional power toggle'). |
| AIB-13 | Subtask splitting orthogonal to sessions: sessions can span subtask boundaries | `partial` | C+ | types/index.ts:250-251 (doc comment: 'Sessions do not have to align to subtask boundaries'), core/scheduler/* (not read — scheduler is out of scope for Phase 1) | Documented design decision but scheduler implementation is not in v1 scope (Milestone 2). No code validates or enforces this yet. |
| AIB-14 | Recurrence: task carries subtask template; each occurrence starts fresh (all unchecked) | `partial` | C+ | types/index.ts:202 (Task.recurrence?: RecurrenceRule), types/index.ts:285-286 (doc comment: 'Each occurrence starts with fresh copy of template') | Type defined but not implemented. Recurrence logic is not built (Milestone 2+ feature). No code generates task occurrences or resets subtask state. |
| AIB-15 | Stakes completion condition: first move, specific subtask, or whole task | `built` | B | types/index.ts:309 (StakeSession.completionCondition: 'first_move' \| 'subtask' \| 'task'), types/index.ts:310 (conditionRefId?: string for subtask id), app/focus/session.tsx:232-238 (completionCondition passed to startStake) | Type and focus session wiring in place. Ignition logic (locking enforcement) is behind IGNITION_NATIVE flag, but the data model supports arbitrary completion conditions per Part 3.10. |
| AIB-16 | Subtask mutations (add/remove/reorder/toggle): each updates parent duration + progressMin | `built` | B | core/task-logic.ts:145-204 (addSubtask, removeSubtask, reorderSubtasks, setSubtaskCompleted all call withSyncedRollups), TaskEditorForm.tsx:247-323 (all subtask handlers use taskLogic functions and patch draft or call store) | Fully implemented. Every mutation recomputes durationMin and progressMin so they never drift. |
| AIB-17 | AI simplify: shrink one subtask to 2-min concrete action (Part 1B.4 rules) | `built` | B | supabase/functions/ai-simplify/index.ts (full implementation), services/ai.ts:215-224 (simplifySubtask with local fallback), TaskEditorForm.tsx:431-447 (UI: 'Make easier' buttons on each uncompleted subtask) | Fully wired. User taps 'Make easier' on a step, AI shrinks it to a 2-minute start. Falls back to 'Just start: {first clause}' when no key. |
| AIB-18 | Fallback (warm generic template): graceful degradation when AI unavailable or quota exhausted | `built` | B | services/ai.ts:104-127 (fallbackBreakdown with crunch/normal sizing), services/ai.ts:182-184 (breakdownTask returns fallback on coerce fail), services/ai.ts:206-207 (refineBreakdown returns prior + isFallback=true when no key) | Implemented. Returns warm generic 4-step template with note 'Showing general steps, not task-specific'. Marked isFallback=true so UI can surface honest banner. |
| AIB-19 | Breakdown memory system: per-user, per-task-type retrieval-augmented personalization (Part 2) | `missing` | C+ | types/index.ts:384-389 (BreakdownMemory type with taskTypeKey, preferredGranularity, commonEdits, completionFidelity), docs/07:192-244 (full Part 2 spec: learning loop, diff capture, style exemplars, low-fidelity patterns) | Type defined but no store, no learning loop, no retrieval. Part 2 requires: (1) capture diff on edit (generated vs accepted), (2) summarize into preferences, (3) store last 1-3 accepted breakdowns as styleExemplars, (4) on next breakdown inject USER_PROFILE + USER_EXAMPLES into prompt, (5) track completion fidelity. This is a deferred feature (FR-70 in Part 4). |
| AIB-20 | Custom breakdown: user writes their own steps, AI cleans up (Part 1C.2) | `missing` | C+ | docs/07:144-168 (full spec: 'Write my own', cleanup contract, preserve wording, no new steps) | No implementation. TaskEditorForm has manual 'Add a step' UI (SubtaskChecklist) but no 'Write my own' mode or AI cleanup pass. Would require: textarea for user steps, cleanup edge function, mode selector in form. |
| AIB-21 | Voice breakdown: user dictates steps, AI transcribes + structures (Part 1C.3) | `missing` | C+ | docs/07:154-156 (spec: speech-to-text, rambly list cleaning, preserve intent) | No implementation. Would require: speech-to-text service (expo-speech-recognition or cloud API), transcript → voice breakdown edge function, UI voice-record button. |
| AIB-22 | Source material attachment: file/photo/voice upload + extraction in any breakdown mode (Part 1.3) | `missing` | C+ | types/index.ts:100-105 (SourceRef type), supabase/functions/ai-breakdown/index.ts:55 (source param ready), services/ai.ts:178 (sourceText param ready), TaskEditorForm.tsx (no file/photo/voice picker UI) | Backend accepts source but UI has no upload affordance. Would require: file picker, image/photo picker, OCR/PDF extraction service (e.g., Tesseract, AWS Textract), on-device extraction fallback. |
| AIB-23 | External context from connected Claude (MCP): optional externalContext param in breakdown (Part 1C.6) | `missing` | C+ | docs/07:174-176 (spec: Claude supplies broader context about user + exact task via optional externalContext) | No implementation. Would require: MCP integration (doc 08) to pass user profile + assignment details from Claude to edge function as externalContext. Deferred until Claude connection is built. |
| AIB-24 | Vague-task handling: raise clarifying question before breakdown, or insert 'decide scope' step (Part 1B.5) | `missing` | C+ | docs/07:127-129 (spec: detect 'too vague' via keywords, surface clarifying question or 'decide scope' step) | No implementation. Would require: keyword detection in task title/notes (essay/study/review/analyze), optional modal asking scope, or auto-include scope-decision step in breakdown. |
| AIB-25 | Validation + repair: check first subtask <= 10 min, first move 2-5 min, regenerate on violation (Part 1.6) | `built` | B | supabase/functions/ai-breakdown/index.ts:87-89 (validation: !firstMove \|\| !subtasks.length returns error), services/ai.ts:134-154 (coerceBreakdown validates each subtask, clamps estimate to 15 if invalid) | Implemented but soft (returns fallback, does not retry). Hard validation (reject invalid, retry) would require edge function to re-prompt on validation fail—currently just errors. |
| AIB-26 | Backward planning from deliverable: model reasons through real requirements, not generic filler (Part 1B.1) | `gated` | B | supabase/functions/ai-breakdown/index.ts:24-34 (SYSTEM prompt instructs backward planning: 'identify real requirements... work backward into steps'), services/ai.ts:104-127 (fallback is generic, not grounded) | Live AI includes instruction; local fallback is deliberately generic 'showing general steps'. Quality depends on source material (doc 07 1.3: 'Accuracy comes from source grounding, not a guess'). Requires implementation of Part 1.3 (source upload) to be effective. |
| AIB-27 | Duration budgeting: subtask estimates sum to parent duration, scaled by user estimation multiplier (Part 1B.2) | `partial` | C+ | docs/07:108-111 (spec: estimates sum to budget, scaled by per-task-type multiplier for time-blindness), core/task-logic.ts:43-46 (sumEstimatedMin), TaskEditorForm.tsx:259 (durationMin = sum of subtasks) | Sum logic built. Estimation multiplier (time-blindness scaling per task-type) is not implemented—treated as 1x. PRD 9.5.6 mentions it but engine not yet built (Milestone 2+). |
| AIB-28 | Calendar rendering: one block per session, 'N steps' chip + progress fill (Part 3.7) | `built` | B | components/calendar/CalendarBlock.tsx:59-62 (remainingSteps count), components/calendar/CalendarBlock.tsx:176-179 ('N steps' chip), components/calendar/CalendarBlock.tsx:108-112 (progressFraction fill) | Fully implemented. One block per ScheduledBlock, chip shows uncompleted step count, progress bar fills as subtasks complete. |
| AIB-29 | Re-breakdown: replace subtask list; preserve progress if exists; confirm with user (Part 3.8) | `partial` | B | TaskEditorForm.tsx:340-380 (applyBreakdown replaces subtasks, resets progressMin to 0), TaskEditorForm.tsx:570-577 (button says 'Re-generate steps' when content exists) | Re-generates and replaces. Spec requires confirm dialog when progress exists ('You have progress on this. Replace the steps?') and preserve completed-equivalent progress—not implemented. Currently always resets progressMin to 0. |
| AIB-30 | Cache breakdown result: keyed by (taskTypeKey, titleHash, sourceHash) so re-open does not re-burn model (Part 1.8) | `missing` | C+ | docs/07:97 (spec: cache keyed by task-type + title + source hash) | No cache implementation. Would require: hash title + source, check cache before calling edge function, store result in MMKV (local-first). Deferred pending memory system (Part 2) since cache key depends on taskTypeKey. |

## docs/08_Claude_MCP_and_API.md — Round C+ MCP Server, Public API, Portable Engine

Docs/08 is a comprehensive spec for an MCP server, public API, and portable engine for Ampora — all deliberately Round C+ deferred. The portable engine (FR-75) is already built and proven: core/ runs both on-device (app) and server-side (edge functions) identically. Phase 5 (Round B) extracts a client-side ToolAction union (7–8 discriminated action types: create_task, update_task, complete_task, create_subtasks, set_first_move, schedule_hint, add_project_phase, update_project_memory) to power agentic project chat with local execution. The full MCP server (FR-73, 40+ tools) and public API (FR-74) are deferred; they would reuse the same tool vocabulary and require OAuth/API-key infrastructure, rate-limiting, and a decision on API shape. No code exists yet for the MCP transport layer, token management, or full-service RPC wrappers.

| ID | Requirement | Status | Round | Evidence / Gap | Notes |
|---|---|---|---|---|---|
| API-MCP-001 | MCP server architecture and transport | `missing` | C+ | No MCP transport, OAuth flow, or token management code exists. No edge function or service implements MCP protocol. | Requires new MCP transport layer (HTTP/SSE), OAuth connector flow or API-key manager in Settings, and an MCP-aware entrypoint. Blocked by decision to build portable engine first. |
| API-TOOLS-001 | Task CRUD tools (create_task, update_task, delete_task, get_task, list_tasks) | `partial` | C+ | Task store methods exist (taskStore.ts), stores persisted to MMKV and Supabase. No tool wrapper layer or RPC endpoints. Doc 08 lists 20+ task operations; only store mutations built. | Core logic exists but must be wrapped in a tool validation + serialization layer. Phase 5 scopes this to client-side ToolAction union only; server lift is C+. |
| API-TOOLS-002 | Task lifecycle tools (complete_task, set_task_progress, break_down_task, refine_breakdown) | `partial` | C+ | break_down_task via supabase/functions/ai-breakdown/index.ts, local fallback in services/ai.ts. complete/progress ops in taskStore. No RPC wrapper or validation schema. | Breakdown engine works on-device and server; needs tool endpoint wrapper and the Phase 5 ToolAction client schema before server lift. |
| API-TOOLS-003 | Subtask tools (add_subtask, update_subtask, complete_subtask, reorder_subtasks, delete_subtask) | `partial` | C+ | Subtask CRUD in taskStore and task-logic.ts. No tool wrapper or validation. Subtasks embedded in Task, synced as part of task. | Mutations exist; need tool schema validation. Phase 5 includes create_subtasks in ToolAction union. |
| API-TOOLS-004 | Event tools (create_event, update_event, delete_event, list_events) | `partial` | C+ | CalEvent type defined (types/index.ts). Calendar views exist (app/calendar/, components/calendar/). No CRUD service or tool wrapper built. | Data model ready; tool layer and store operations need implementation. Calendar UI is display-only so far. |
| API-TOOLS-005 | Schedule tools (get_schedule, rebuild_schedule, recovery_replan, reschedule_block) | `partial` | C+ | Scheduler core complete (core/scheduler/). recovery.ts exists. scheduleStore for blocks. Home + Calendar display blocks. No RPC endpoints to retrieve or manipulate schedule. | Engine fully built and deterministic. Need tool wrappers to serialize blocks + dates and expose recompute/recovery. reschedule_block is pin-to-time, partially done via UI drag. |
| API-TOOLS-006 | Organization tools (create_list, list_lists, update_list, delete_list, list_tags, list_scheduling_hours) | `partial` | C+ | listStore (Zustand + MMKV), SchedulingHours type, tag filtering in tasks. No CRUD service layer or tool endpoints. | Types and stores exist; tool layer and validation schema needed for server lift. |
| API-TOOLS-007 | Settings/configuration tools (get_settings, update_setting safe subset, list_scheduling_hours) | `partial` | C+ | settingsStore (Zustand + MMKV), SchedulingHours type in types/index.ts. Settings UI in app/settings/. No RPC endpoints to read/write safely. | Stores built; need tool schema to enforce 'safe subset' (e.g., no remote disable of wellbeing caps). Phase 5 does not include settings in ToolAction. |
| API-TOOLS-008 | Stakes tools (list_stake_apps, attach_stake, list_stake_sessions; no remote lock) | `partial` | C+ | stakesStore (Zustand + MMKV), stake attachment in task editor, Ignition soft-lock in place. No remote API — correctly scoped read/configure only. Doc 06 confirms lock happens on-device. | Soft-lock flow is local-only and safe. Need tool layer to read/configure stakes. Remote lock boundary enforced by architecture (no tool to engage the lock). |
| API-TOOLS-009 | Learning tools (get_focus_profile) | `partial` | C+ | learningStore (Zustand + MMKV), focusDna.ts, revealedSelf.ts, energy.ts in core/learning/. Profile data computed and stored. No RPC to read it. | Engine complete; need tool endpoint to serialize and return Focus DNA (best windows, multipliers, dodged types). |
| API-PORTABLE-001 | Portable engine — pure, deterministic, I/O-free TS module | `partial` | B | core/ folder: scheduler/index.ts, blocking/, calendar/, learning/, recovery.ts, task-logic.ts, quick-add.ts, subscription.ts. All functions take inputs and return outputs, no I/O calls. Used by app and edge functions. | Engine is already portable and runs on-device + in edge functions (ai-breakdown, ai-project-chat use the same logic). No packages/ dir needed — core/ already serves both app and functions. |
| API-PORTABLE-002 | Packages structure for engine distribution (packages/engine, packages/breakdown) | `missing` | C+ | No packages/ directory. Portable logic lives in core/ alongside the app code. Doc 08 §1 shows the intended structure but it is not built. | Doc 08 shows desired layout; implementation could keep core/ and import it, or extract to packages/ for clarity and reuse. Decision deferred — currently working because both app and functions live in same repo. |
| API-PHASE5-001 | Phase 5 client-side ToolAction union schema | `missing` | B | No types/ToolAction.ts or core/ai-actions.ts. Plan specifies: create_task, update_task, complete_task, create_subtasks, set_first_move, schedule_hint, add_project_phase, update_project_memory. | Phase 5 task: build discriminated union in types/index.ts + core/ai-actions.ts with manual Zod-style validation. This is the client-side tool vocabulary for agentic project chat before server lift. |
| API-PHASE5-002 | Client executor for ToolAction (services/aiProjects.ts + ProjectChat.tsx) | `missing` | B | services/aiProjects.ts exists (projectChat, generateNextTask) but no action executor. ProjectChat.tsx renders messages but not action confirmation chips or undo. | Phase 5 task: extend aiProjects.ts to parse { reply, actions: ToolAction[] } from Gemini. Apply actions via store APIs. Render confirmation chips with Undo (store inverse ops). ProjectChat.tsx shows confirmation UI. |
| API-PHASE5-003 | AI project-chat upgraded to JSON mode with action vocabulary | `partial` | B | supabase/functions/ai-project-chat/index.ts exists, returns { text: string }. No JSON mode, no action schema in system prompt. | Phase 5 task: add responseMimeType: 'application/json' to Gemini call, teach system prompt the action vocabulary, parse { reply, actions } shape. Keep no-key contract (200 {error:'no_key'}). |
| API-PHASE5-004 | Thread history UI + scrollback (Project.chat persistence) | `partial` | B | Project.chat is persisted (types/index.ts Project.chat: ChatMessage[]). Components/projects/ProjectChat.tsx renders live messages. No scrollback, no day-dividers, no 'Clear conversation'. | Phase 5 task: render all Project.chat history on open. Add day dividers. Add menu option to clear. No new thread model — stays project-scoped. |
| API-PHASE5-005 | Extend action pattern to task breakdown 'Refine' | `missing` | C+ | Task editor break-down button calls ai.ts functions. No action parsing or refinement UI. No Refine action in Phase 5 ToolAction union. | Phase 5 optional: same action pipeline (update_task, create_subtasks actions) for inline refinement. Not in round-B critical path. |
| API-PHASE5-006 | Local fallback for agentic actions (extractTasks parser) | `partial` | B | core/quick-add.ts has NL parser (extractTasks). services/aiProjects.ts has fallback chat + task generation. No fallback for the action pipeline itself. | Phase 5 task: when no key, chat's 'turn into tasks' falls back to deterministic extractTasks → same action pipeline so offline action execution works. |
| API-PHASE5-007 | Ready-for-key deployment checklist (6 functions deployed, no key needed) | `partial` | B | ai-breakdown, ai-simplify, ai-refine, ai-extract-tasks, ai-project-chat, ai-project-task all exist in supabase/functions/. Shared gemini.ts. All return no_key response. | Phase 5 task: verify all 6 functions handle no-key gracefully (already done). Deploy all 6 to Supabase (Phase 5 step 6). Requires supabase CLI or MCP. Aria activates by adding GEMINI_API_KEY secret. |
| API-MCP-002 | OAuth connector flow + API-key management in Settings | `missing` | C+ | No Settings screen UI for API key generation or management. No OAuth integration. auth.tsx handles sign-in/sign-up only. | Requires Settings page extension to generate/revoke scoped API keys and OAuth client setup. Round C — deferred until MCP server architecture decision. |
| API-MCP-003 | Conflict log and multi-device sync UI | `partial` | C+ | store/syncStore.ts has last-write-wins merge logic. No user-facing conflict-resolution UI. PRD 9.12 mentioned. | Sync works silently; true multi-device conflict UI (which version to keep) is Round C. Current app assumes single-device primary use. |
| API-MCP-004 | Rate-limiting and audit log for API/MCP writes | `missing` | C+ | No rate-limit middleware, no audit log persistence. Supabase auth tokens in SecureStore only. | Requires Supabase RLS policies, rate-limit middleware, and an audit table. Out of scope for portable engine build. |
| API-VOCAB-001 | Doc 08 tool vocabulary — consolidated reference | `built` | C+ | docs/08_Claude_MCP_and_API.md §2.3 defines full tool surface: 40+ tools across Tasks, Events, Schedule, Organization, Stakes, Learning. All map to existing app features or proposed RPC endpoints. | Documentation is complete and authoritative. Vocabulary is locked. Phase 5 ToolAction is a subset optimized for client-side agentic actions; full MCP server implements all 40+ in Round C. |
| FR-73 | FR-73: MCP server with OAuth/API-key auth, user-data only, stakes read/configure | `missing` | C+ | No MCP server or token manager. App auth exists (Supabase magic-link). No remote-lock tooling (correctly omitted). | Phase 5 builds the client-side tool vocabulary; MCP server implementation is deferred. Requires portable engine decision and OAuth flow design. |
| FR-74 | FR-74: Public API (REST or GraphQL) with API-key auth and rate limits | `missing` | C+ | No REST/GraphQL endpoints. No API-key generation or validation. No rate-limit infrastructure. | Fully deferred to Round C. Would mirror the MCP tool surface with HTTP/REST transport. Requires infra decisions (OpenAPI schema, endpoint layout, Supabase RLS policies). |
| FR-75 | FR-75: Portable engine (pure TS, no platform deps, runs app + edge functions identically) | `built` | B | core/ folder (scheduler/, blocking/, calendar/, learning/, recovery.ts, task-logic.ts, quick-add.ts, subscription.ts) is pure, deterministic TS with no I/O. Used by app (Zustand) and edge functions (Deno). Zero platform deps. | Engine is already portable and proven (app + ai-project-chat both use same recompute logic). No refactoring needed. Phase 5 keeps this pattern intact for ToolAction executor. |

## Projects (doc 10_Projects.md, PR FR-82..86)

Projects v1 implementation is fully built and shipped. The project model is complete with file knowledge base, three progress kinds (deliverable/study/general), scoped conversational chat, memory tracking, and task generation. All five core requirements (FR-82..86) are implemented: distinct project type (hub + detail screens), chat-as-planner with "Add steps to plan" affordance, progress awareness (phases/topics/percent), local-first memory storage, and task linking for scheduler+Ignition integration. Cloud sync to Supabase is documented as a later increment (doc §13) and not blocked by this ship. AI fallbacks are in place (edge functions + local templates). No regressions detected.

| ID | Requirement | Status | Round | Evidence / Gap | Notes |
|---|---|---|---|---|---|
| PRJ-model | Project data model (types + store) | `built` | B | types/index.ts:488-504 Project interface with name/kind/description/files/progress/chat/memory/taskIds; store/projectStore.ts:79-150 with createProject/updateProject/deleteProject/addFile/addChatMessage/setProgress/linkTask actions | BaseEntity (id/createdAt/updatedAt/syncState) present; initial progress correctly discriminated by kind; MMKV persistence with persist key 'ampora-projects' |
| FR-82 | Projects are first-class, explicitly created, with multi-file knowledge base | `built` | B | app/projects/index.tsx (hub), app/projects/[id].tsx (detail), components/projects/NewProjectSheet.tsx (create sheet), components/projects/FileList.tsx (file picker + paste text), types/index.ts:430-440 ProjectFile with name/type/uri/extractedText/addedAt | File upload via DocumentPicker (PDF/text/images) + text paste. Plain-text extraction working; OCR/PDF extraction deferred (doc §13). Files stay with project; back-reference via Project.files array |
| FR-83 | Scoped chat interface (answer + act); universal repair mechanism | `built` | B | components/projects/ProjectChat.tsx (chat UI + send + step parsing), services/aiProjects.ts projectChat() → ai-project-chat edge function (supabase/functions/ai-project-chat/index.ts), fallback templated reply grounded in project state. 'Add steps to plan' affordance at ProjectChat.tsx:139-156 creates real Tasks via createTask+linkTask | Chat persisted in Project.chat; parsePlanSteps() recognizes bullet/numbered lists; steps create Tasks with projectId back-ref. Fallback reply reads project name/kind/progress/files so chat never surfaces error |
| FR-84 | Progress awareness + next-session generation (phases/topics/percent, FirstMove for current position) | `built` | B | types/index.ts:465-468 ProjectProgress (discriminated union by kind); components/projects/ProgressTracker.tsx editable progress UI (phases/topics/general); components/projects/ProgressRing.tsx ring + percent label; app/projects/[id].tsx 'Plan my next session' action calls generateNextTask() → ai-project-task edge function (supabase/functions/ai-project-task/index.ts); fallback generates context-aware subtasks per project.progress.kind | Deliverable: phases with 0-100% each; study: topics with mastery 0..1; general: single percent. Session budget default 45min. FirstMove is 2-5min for THIS session, not whole project. Subtasks max 8 per doc §7 |
| FR-85 | Project memory (decisions, style, weak spots) — local-first, no shared-model training | `built` | B | types/index.ts:500-501 Project.memory: string[] (entries stored locally); store/projectStore.ts setProgress() updates memory via store mutation. Doc §10 notes this is per-user/per-project, local-first, not used for shared model training | Memory array persisted in MMKV with the project. Chat can reference/update memory via the payload sent to edge function (services/aiProjects.ts projectPayload includes project.memory). Future work: breakdown memory details from doc 07 Part 2.1 |
| FR-86 | Projects feed scheduler + Ignition (generate Tasks as schedulable/lockable units) | `built` | B | types/index.ts:193-194 Task.projectId?: string (back-ref); store/projectStore.ts linkTask() adds taskId to Project.taskIds; app/projects/[id].tsx planNextSession() creates real Task via createTask(), adds subtasks, calls linkTask(). Generated Task has autoSchedule=true, projectId, firstMove, subtasks → flows into scheduler | Doc §7/§9: Project generates Tasks (the schedulable unit); user locks against Task (never project). Task.projectId links back for audit. Session-scoped FirstMove defeats activation energy per doc 07 Part 1D |
| PRJ-hub-screen | Projects hub (list, empty state, new project affordance) | `built` | B | app/projects/index.tsx: header with back button + rocket icon, FlashList of sorted (most-recent-first) ProjectCard items, FAB + NewProjectSheet, EmptyState 'Projects are bigger than tasks' with CTA. ProjectCard.tsx: name + kind badge + progress ring + file/task counts | Accessibility: role='button', labels on all controls. Header icon uses PROJECT_ACCENT #7C3AED. Empty state teaches the Projects concept. Sorting by updatedAt keeps active projects top |
| PRJ-detail-screen | Project detail (header, progress tracker, files, chat tabs) | `built` | B | app/projects/[id].tsx: header (back, delete), progress ring + name + kind badge + file/task counts, 'Plan my next session' card (primary action), three tabs (Progress/Files/Chat). Tab content: ProgressTracker.tsx, FileList.tsx, ProjectChat.tsx. PlannedSheet modal shows session result with FirstMove + steps | Tabs swap content via state; each tab handler calls appropriate store action (setProgress/addFile/addChatMessage). Delete confirmation modal. Planned session modal offers 'Open task' and 'Start focus' CTAs |
| PRJ-progress-tracker | Progress visualization (editable, type-specific UI) | `built` | B | components/projects/ProgressTracker.tsx: DeliverableTracker (phase bars, tap to cycle 0→25→50→75→100), StudyTracker (5-dot mastery), GeneralTracker (ring + ±10% steppers). Never color-only: all carry text labels + numeric values | Deliverable phases removable/addable; study topics with mastery levels 1-5 (0.2 per level); general has +/- buttons. All changes go through onChange callback to store |
| PRJ-file-list | File knowledge base UI (upload, paste, preview, remove) | `built` | B | components/projects/FileList.tsx: DocumentPicker (PDF/text/*), ImagePicker (native photos), AddTextSheet for paste. fileTypeFromMime() classifies; extractTrivialText() reads plain-text files; file preview shows name + extracted text. Remove via trash icon | Plain-text extraction working; OCR/PDF text extraction deferred to later (doc §13). Empty state explains RAG. Try/catch guards prevent crashes on unsupported platforms. Files persist in Project.files |
| PRJ-chat | Chat interface (send, history, step parsing, 'Add to plan') | `built` | B | components/projects/ProjectChat.tsx: ScrollView message list, composer with TextInput + send button, parsePlanSteps() for bullet/numbered lists, MessageBubble with addStepsToPlan affordance. Suggestion chips on first message. Fallback note on offline replies | Chat persisted via onAppendMessage into Project.chat. Plan-step parsing graceful (returns [] if no match). Max 8 steps per doc 07. Accessibility: labels on all controls. Emoji-free copy |
| PRJ-ai-chat-edge-fn | Edge function: ai-project-chat (Gemini backend with fallback) | `built` | B | supabase/functions/ai-project-chat/index.ts: receives project payload + message, calls callGemini() (doc §5 system prompt), returns 200 { text } on success or 200 { error: 'no_key' } if GEMINI_API_KEY unset | System prompt instructs 'brief, concrete, next step grounded in project state'. Temperature 0.5. Max 700 tokens. Client-side fallback in services/aiProjects.ts projectChat() handles soft errors |
| PRJ-ai-task-edge-fn | Edge function: ai-project-task (session generation with fallback) | `built` | B | supabase/functions/ai-project-task/index.ts: receives project + sessionMin, returns { title, firstMove, subtasks[] } (max 8). callGemini with STRICT JSON system prompt. Returns error if key unset; extractJson() parses result | FirstMove is 2-5 min for THIS session. Subtask estimates default to 15 if not finite. System prompt emphasizes deliverable/study/general kind-specific logic. Fallback in services/aiProjects.ts generateNextTask() |
| PRJ-fallbacks | Local fallbacks (grounded in project state, never generic) | `built` | B | services/aiProjects.ts: fallbackChatReply() includes project name/pct/nextFocus/fileCount; fallbackNextTask() generates context-aware session per kind (deliverable/study/general). Both marked isFallback=true for UI note | No generic 'try again' responses. Fallback replies point at concrete next steps. When AI unavailable, app still responds warmly with real project context |
| PRJ-task-linking | Task ↔ Project back-references (projectId + taskIds) | `built` | B | types/index.ts: Task.projectId?: string (line 194); Project.taskIds: string[] (line 503). store/projectStore.ts linkTask() idempotent append. app/projects/[id].tsx planNextSession() sets projectId + calls linkTask(). ProjectCard displays taskIds.length | When project chat 'adds steps to plan', each step creates Task with projectId. Project.taskIds tracks generated tasks (for display, for future 'archive/clear' operations). Bidirectional link enforced |
| PRJ-design-tokens | Design system compliance (colors, spacing, motion, accessibility) | `built` | B | components/projects/projectUtils.ts PROJECT_ACCENT #7C3AED (special/premium accent per doc 02). All components use NativeWind + design-tokens.ts. ProgressRing never color-only (percent label required). Buttons, cards follow doc 02. Accessibility: all controls have accessibilityLabel + role | No hardcoded colors/spacing. Motion: FadeInDown staggered list enter. Reduce-motion respected. Touch targets ≥48px. Tabs/buttons follow design system state cycles |
| PRJ-store-sync-state | Store sync state tracking (MMKV persistence + syncState field) | `built` | B | store/projectStore.ts: persist() with mmkvStateStorage (key 'ampora-projects'). patchProject() sets syncState='pending' on every mutation. BaseEntity fields (id/createdAt/updatedAt/syncState) present in Project type | Local-first MMKV write; cloud sync is a later increment (doc §13). syncState='pending' on mutation; no Supabase calls in projectStore today. Pattern matches taskStore/listStore |
| PRJ-cloud-sync-gated | Cloud sync to Supabase (deferred, documented) | `gated` | C+ | store/projectStore.ts comment line 12: 'Cloud sync is a later increment (doc `10` §13); nothing here calls Supabase directly.' doc/10_Projects.md §13: 'Projects are a later milestone... Add Projects after... knowledge base (RAG over files) and the project chat with tool use are the two heavier pieces and can ship incrementally' | Deferred blocker: Supabase migrations for projects table + row-level security. Service layer (supabase.ts) will add projectStore sync mappers later. No regression: local-first MMKV works offline |
| PRJ-rag-retrieval | RAG retrieval over project files (chunk embeddings + context window management) | `gated` | C+ | types/index.ts: ProjectFile comment line 427: 'index?: EmbeddingRef; // chunk embeddings for retrieval (doc 10 §13)' — field defined but unused. FileList.tsx line 280: 'OCR and PDF text extraction arrive in a later update' | Deferred blocker: embedding infrastructure + retriever. Edge functions receive fileNames[] only; full file text retrieval planned. Current: small corpora passed as direct context; retrieval/chunking later |
| PRJ-project-memory-rich | Breakdown memory details (avgSubtaskCount, styleExemplars, lowFidelityPatterns) | `gated` | C+ | types/index.ts comment line 375-382: 'doc 07 Part 2.1 additionally specifies... the minimal §9.4 shape is implemented here; the doc 07 fields can be added when the memory system itself is built' | Deferred blocker: breakdown memory system (Milestone covering FR-70). Current: Project.memory is string[] for decisions/style/weak-spots; full per-task-type breakdown memory added later |
| PRJ-tool-use-chat | Chat as tool-calling agent (create/modify/reschedule tasks via chat) | `partial` | C+ | services/aiProjects.ts projectChat() is Q&A + step proposal; ProjectChat.tsx 'Add steps to plan' converts proposed steps into Tasks. Full tool-use (agent modifies existing tasks, reschedules, updates progress) deferred per doc §5 'in future' | Current: one-shot 'propose steps' → user clicks 'Add'. Full agent with ToolAction vocabulary (doc 08 Phase 5) planned later. Universal repair mechanism (doc §10) partially working: chat + manual UI edits suffice for now |
| PRJ-empty-state-teaching | Empty state teaching (Projects vs Tasks distinction) | `built` | B | app/projects/index.tsx line 95-103: EmptyState icon='rocket-outline' title='Projects are bigger than tasks' subtitle explains paper/exam/ongoing, actionLabel='New project' | First-time user education built in. Icon + copy + CTA teach the concept before creation |
| PRJ-kind-meta | Project kind metadata (label, icon, blurb per type) | `built` | B | components/projects/projectUtils.ts kindMeta(): deliverable/study/general each with label + icon + blurb. NewProjectSheet.tsx displays all three with radio buttons + descriptions. ProjectCard.tsx displays kind badge | Blurbs: deliverable='paper or assignment'; study='exam or unit'; general='ongoing work'. Icons: document-text, school, albums |
| PRJ-newproject-sheet | New project creation sheet (name + kind + optional description) | `built` | B | components/projects/NewProjectSheet.tsx: TextInput for name (required), radio chooser for kind (deliverable default), multiline TextInput for description (optional). Backdrop-press guard handles web keypress bubbling. onCreate callback creates project + navigates to detail | Name autofocus. Kind defaults to deliverable. Description can be omitted. Web-safe: target===currentTarget check prevents Space in TextInput from closing sheet |
| PRJ-deletion | Project deletion (with confirmation, keeps generated tasks) | `built` | B | app/projects/[id].tsx: trash icon opens ConfirmDeleteSheet, deleteProject() removes project but tasks persist (doc comment line 453: 'Tasks it generated stay in your task list') | Tasks with projectId set remain after project delete; they lose project context but are not cascade-deleted (by design) |
| PRJ-icon-consistency | Icon usage (rocket for projects, consistent with design language) | `built` | B | app/projects/index.tsx header icon: rocket-outline. ProjectChat.tsx onboarding icon: map-outline. All icons use @expo/vector-icons.Ionicons from design-tokens.ts iconSizes | No custom SVGs; all Ionicons. Sizes from design-tokens. Consistent with design language |
| GAP-voice-breakdown | Voice input for project chat / task breakdown (doc 07, out of scope) | `missing` | C+ | doc 07 Part 1.1 mentions voice as one breakdown mode (prose/form/voice/custom); no implementation in services/ai.ts or edge functions | Deferred: microphone input pipeline, speech-to-text integration, voice-trained models. Current fallback: text input only |
| GAP-quiz-generation | Quiz mode for study projects (doc 10 §5 'quiz me', §8 mastery from quiz) | `missing` | C+ | doc 10 §5 example 'Quiz me on X' and §8 'updates mastery from quiz performance' — no quiz edge function or UI in current ship. ProgressTracker mastery is manual tap-based only | Deferred: quiz generation, answer grading, mastery update from performance. Current: manual mastery entry only (5 dots) |
| GAP-project-linking-ui | Visual link between project and its generated tasks (project detail → task preview / task detail → project link) | `partial` | C+ | Project detail shows taskIds.length in the header. Task editor could display 'Linked to project X' but doesn't today. No task → project navigation UI | Current: back-ref bidirectional (Task.projectId ↔ Project.taskIds). UI navigation only from project→task. Reverse link (task→project) deferred |

## Open GAPs from docs/11_Review_and_Open_Gaps.md Part C (Plan v2 §2.6)

Round B audit of 14 open GAPs from the plan (§2.6, referencing docs/11 Part C/D). Most GAPs are already resolved or have code implementing the recommended default. Six are already built (day-capacity definition, pinned-vs-fixed-event priority, undated-backfill, multi-device-stakes, beat-the-clock cooldown, sync-conflict); three require minor surface work within scope (notification-permission-denied UI nudge, project file limits enforcement, delete-project-tasks confirmation); three are legitimately deferred (monetization feature-limits, privacy policy, timezone/travel recompute breadth); two require clarification (event-crossing-midnight rendering, task-in-multiple-projects model confirmation).

| ID | Requirement | Status | Round | Evidence / Gap | Notes |
|---|---|---|---|---|---|
| GAP-day-capacity-definition | Day-capacity definition for load balancing | `built` | B | core/scheduler/placement.ts:169-179 dayCapacityMinutes() computes capacity as free-slot minutes on a day; placement.ts:170-222 bestWindow() applies K_LOAD penalty using dayCapacityMinutes / placed-per-day ratio for balanced mode. | Resolves to: scheduling-hours minutes on that day minus fixed events that day (via freeTime.ts free-interval construction). Confirmed in code. |
| GAP-pinned-block-vs-new-fixed-event | Pinned block vs newly added overlapping fixed event priority | `built` | B | core/scheduler/freeTime.ts:1-14 comment states subtraction order: (1) CalEvents, (2) fixed tasks, (3-5) scheduling hours/quiet hours/pinned blocks. Pinned blocks are computed into free time AFTER fixed events, so fixed events win. | Resolves to: the fixed calendar event (real busy time) wins; the pinned task block reflows with the pinned-minutes credit applied to remaining work. Inline notification of the move is UI/Phase 2 work (not yet visible on calendar drag). |
| GAP-over-subscription-drop-priority | Over-subscription: which tasks get dropped and how is it surfaced | `built` | B | core/scheduler/placement.ts:291-346 placeTask() appends unplaced work to state.unschedulable[] with a reason; core/scheduler/types.ts:61-64 Unschedulable interface; core/scheduler/recompute.ts returns unschedulable list as part of ScheduleResult. | Resolves to: keep highest-priority + earliest-due, surface the rest in unschedulable list with reason (FR-20). Code credits remaining work correctly via pinnedMinutesByTask. |
| GAP-event-crossing-midnight | Events crossing midnight and multi-day rendering | `partial` | B | components/calendar/hours.ts uses startOfDay/endOfDay helpers (midnight boundaries); DayView/ThreeDayView/WeekView/AgendaView all use local-midnight epoch logic. No evidence of multi-day block rendering (split at day boundary in Day/3-Day/Week). | Resolves to: render split at day boundary. Calendar blocks on a single task do not currently render as multi-day events; Phase 2 (calendar drag overhaul) scope expands to handle cross-midnight blocks via visual split. |
| GAP-timezone-travel | Timezone changes and travel (recompute on tz change) | `missing` | C+ | No grep matches for 'timezone' or 'travel' in core/scheduler or app/. The scheduler uses local-day logic (startOfDay via Date#getDay) but no detection or recompute trigger on timezone/travel change. | Resolves to: recompute on tz change; blocks are wall-clock-local. Deferred (requires monitoring device timezone changes and triggering recompute). Low priority for v1. |
| GAP-notification-permission-denied | Notification permission denied graceful degradation | `partial` | B | services/notifications.ts:90-115 requestNotificationPermission() returns false on denied; no-op paths throughout. No Settings nudge or one-time prompt on permission denial detected in onboarding/settings code. | Resolves to: add one-time Settings nudge on first denial, no nagging. Already degrades to in-app/web. Phase 4 onboarding/settings passes should add a single gentle nudge (low-effort). |
| GAP-project-file-limits | Project file storage limits (count, size, types) | `missing` | B | types/index.ts:430-440 ProjectFile type has no limit annotations. No validation in projectStore or file-upload component. Phase 6 scope mentions FileList but no limits enforced in the codebase. | Resolves to: default 20 files/project, 25 MB/file, types pdf/img/doc/sheet. Phase 3 or 4 scope: add upload validation in file-picker component (enforce sizes/types silently; reject oversized files with an inline message). |
| GAP-delete-project-tasks | Deleting a project: what happens to its generated tasks | `partial` | B | store/projectStore.ts:111-118 deleteProject() removes the project but does not touch tasks. No dialog or option to also delete tasks is present. | Resolves to: default keep + unlink (don't destroy work); confirm dialog offers 'also delete tasks'. Phase 3/4: add confirmation modal on project delete with a checkbox for cascading task delete. |
| GAP-task-in-multiple-projects | Can a task belong to more than one project | `built` | B | types/index.ts:194 Task has 'projectId?: string' (singular, optional). types/index.ts:502 Project has 'taskIds: string[]' (many). No projectIds[] array on Task; 1:N model enforced. | Resolves to: No — one project per task (recommended in doc 11). Code enforces this via singular projectId on Task. |
| GAP-sync-conflict-whole-day | Sync conflict resolution for a whole day's schedule | `built` | B | store/syncStore.ts:16-21 merge rule: 'compare updatedAt (epoch ms). The NEWER copy wins; on tie LOCAL wins.' Applied per-field/per-entity, not block-by-block. | Resolves to: last-write-wins per field (built). Add quiet 'schedule updated on another device' notice (UI polish, Phase 4). |
| GAP-undated-auto-scheduled | Undated tasks: auto-scheduled handling | `built` | B | core/scheduler/placement.ts:145-146 'Upper bound: due if present, else cutoff (undated backfill).' Doc 11 D8: 'RESOLVED (9.5.11 backfill, never displaces dated). Confirm only.' | Resolves to: not force-placed; stay on list, optional lowest-priority backfill, never displace dated work. Confirmed built. |
| GAP-multi-device-stakes | Multi-device stakes and beat-the-clock cooldown | `built` | B | types/index.ts:304-322 StakeSession has 'deviceId: string' (per-device). Doc 11 D5/D6: 'Each device independent; cooldown ends early if task completed (FR-41).' core/blocking/SoftBlockingStrategy.ts implements this. | Resolves to: each device independent, no multi-device lock (FR-41b). Cooldown ends early on completion (FR-41). Both confirmed built. |
| GAP-monetization-limits | Monetization: feature limits per tier (AI/projects/stakes) | `built` | C+ | Doc 11 D2: 'Paid app, no free tier (doc 11 D2) — so no per-feature gating needed for v1.' core/subscription.ts models trial/active/lapsed but no per-feature caps. | Resolves to: paid app, no free tier for v1 (no per-feature gating needed). Deferred to Round C if a Free tier is ever wanted. Confirm with Aria. |
| GAP-privacy-tos | Privacy policy and terms of service | `missing` | C+ | No privacy policy, terms of service, or legal document found in the repo. Docs reference COPPA 13+ (NFR-4, doc 6) and mention minors, but no legal text is present. | Resolves to: required for launch (Apple requires). Flagged as a legal task outside code scope — cannot ship until completed. This is a Round C external blocker (requires legal/founder input). |
