{
  "$comment": "Read by scripts/native-toggle.js's `printOnInstructions()`, grouped by native.config.json flag. Lists npm packages a native build needs, so a Windows `npm install` never sees them (they are deliberately absent from the root package.json). The toggle script only PRINTS `npx expo install ...` lines from this file; it never runs them.",
  "groups": {
    "ignition": {
      "description": "native/modules/ampora-ignition is a from-scratch Expo module written directly against Apple's FamilyControls / ManagedSettings / DeviceActivity / ManagedSettingsUI frameworks plus expo-modules-core, both already present once `expo` itself is installed (which it is, on every branch). It does NOT wrap react-native-device-activity or expo-app-blocker (docs/05 §2 names those as an alternative starting point; this build went with the homegrown module instead so the exact session/hold/trigger model in docs/04 maps 1:1 without fighting a third-party package's own opinions). There is therefore nothing third-party to install for ignition today.",
      "dependencies": {}
    },
    "purchases": {
      "description": "Owned by the RevenueCat purchases workstream (ARCH_PLAN Part B7 / owner override: react-native-purchases, not expo-iap). core/iap/NativePurchaseStrategy.ts wraps this package's default export (configure/getOfferings/purchasePackage/restorePurchases/getCustomerInfo), resolved via a lazy require() call, never a static import (see that file's docstring). Metro's optional-package alias in metro.config.js already reserves 'react-native-purchases' for it. Version 10.5.0 is the latest published as of this writing (verified via `npm view react-native-purchases version`); confirm compatibility with the installed Expo SDK before `npx expo install`-ing it, since this repo could not run that command itself (Windows, no native build).",
      "dependencies": {
        "react-native-purchases": "^10.5.0"
      }
    }
  }
}
